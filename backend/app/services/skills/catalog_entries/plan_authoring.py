# Copyright (c) 2026 Valaris Studio
# SPDX-License-Identifier: AGPL-3.0-or-later

"""Catalog entry: Plan Authoring (generated from the verified source bundle;
do not hand-edit the embedded content)."""

from app.services.skills.catalog_types import CatalogEntry, CatalogFile

ENTRY = CatalogEntry(
    catalog_id='plan-authoring',
    catalog_version=2,
    name='Plan Authoring',
    description="Authors a project's committed grounding docs — research doc with option matrices and plan doc with Locked Decisions, phased epics-to-cards, test-first DoDs, and an integration layer. Use to write or audit a PLAN.md, RESEARCH.md, or SPEC.md.",
    files=(
        CatalogFile(
            path='SKILL.md',
            content='---\nname: Plan Authoring\ndescription: Authors a project\'s committed grounding docs — research doc with option matrices and plan doc with Locked Decisions, phased epics-to-cards, test-first DoDs, and an integration layer. Use to write or audit a PLAN.md, RESEARCH.md, or SPEC.md.\ntoolsets: [start-here, work-management, knowledge-content]\n---\n\n# Plan Authoring\n\nTurn a brief — or a pile of unorganized spec material — into the two\ncommitted documents that ground a delivery: a **research doc** (the\nevidence) and a **plan doc** (the decisions and the card-ready\nbreakdown). Grounding quality is the single highest-leverage input to\nrun quality: cost-per-card and reversion rate track spec quality more\nthan any pipeline tuning does. The same docs serve human-run projects;\nthe bar here is set by what an autonomous runner needs, which is\nstrictly higher.\n\nThe skill structures the documents; the **domain research itself** is\nthe agent\'s/human\'s work — this skill tells you what shape the findings\nmust land in, not what to find.\n\n**Covers:** authoring the research doc and the plan doc, the Locked\nDecisions discipline, the phased epics→cards breakdown with test-first\nDoDs, designing the integration/acceptance layer at plan time, citation\ndiscipline, committing the docs, and a review mode for auditing an\nexisting plan\'s run-readiness.\n\n**Does not cover:** creating cards, boards, or any board entity — this\nskill produces files; the board setup process consumes the epics→cards\ntable when authoring cards. Nor the domain research itself — option\ndiscovery, benchmarks, vendor evaluation; this skill consumes those\nfindings into a fixed structure.\n\n## 1. Inputs\n\n- **The brief** (or the unorganized material to absorb — existing docs,\n  chat dumps, READMEs).\n- **The target repo** — the docs live versioned *in the repo*, not on\n  the board.\n- **The board definition** (`get_definition`): constraints, exclusions,\n  decisions already made, and the project\'s **doc-path convention**.\n  Default convention: `docs/RESEARCH.md` + `docs/PLAN.md` (or\n  `docs/SPEC.md` for the consolidated shape, section 3); if the\n  definition\'s `references` record different paths, those win. If no\n  board exists yet (plan authoring can precede board creation), use the\n  defaults and note in the handoff that step 7\'s definition write\n  happens once the board is created.\n- Any existing PLAN/RESEARCH to extend — read it fully first; section\n  6\'s conflict rule applies.\n\n## 2. The research doc — evidence, opinionated\n\nOne section per subsystem or major decision area. Per section:\n\n- **Option matrix**: a table of candidates × the criteria that actually\n  discriminate (platform constraints, licensing, maturity, integration\n  cost), with a verdict column. Dead-end options stay in the table,\n  marked — they prevent re-research.\n- **Recommended approach** for v1, stated plainly. Research is allowed\n  to be opinionated; it feeds the plan\'s Locked Decisions.\n- **Reusable references**: projects/libraries worth studying, each with\n  its license — flag copyleft licenses as study-only where the project\'s\n  licensing posture requires it.\n- **Risks / open questions** for that subsystem.\n\nClose with a **master source list**. Structure reference with skeletons:\n`references/doc-structures.md`.\n\n## 3. The plan doc — decisions and the card-ready breakdown\n\nNumbered sections, in this order (skeleton in\n`references/doc-structures.md`):\n\n1. **§0 Locked Decisions** — a table: Area | Decision | Why (citing the\n   research doc). *Locked* has an operational meaning: a runner (or\n   anyone) hitting a conflict with a locked row must **escalate to a\n   human, never silently rewrite** — write that sentence into the\n   section so every reader knows the contract.\n2. **§1 Target architecture** — component diagram; where the product\n   has a lifecycle, the **state-machine spine** (states + transitions) —\n   it becomes the backbone cards anchor to.\n3. **§2 Repo layout** — the intended tree with one-line notes.\n4. **§3 Phased epics → cards** — the card-authoring feedstock. Epics\n   (A, B, C…) in dependency order; under each, card rows (A1, A2, …)\n   each carrying: **goal** (one line), **test-first DoD** ("write\n   <test> covering <behavior>; implement until green" — an untestable\n   row is unfinished planning), **likely files**, **out-of-scope**.\n   These row fields are the plan-sourced half of the card anatomy: card\n   authoring adds Context/Scope/Acceptance Criteria derived from them,\n   with no invention needed.\n5. **§4 Sequencing & milestones** — phase gates tying epics to\n   checkable outcomes ("M1: it records").\n6. **§5 Top risks** — carried forward onto every board, not buried.\n\nFor service/API products, prefer the **consolidated spec shape**\n(`references/doc-structures.md` shape B, at `docs/SPEC.md` unless the\ndefinition says otherwise; citations then read `docs/SPEC.md §N`). Its\ndistinguishing block is the **operational decisions** section (auth,\nidentifiers, ingestion, failure handling, deployment posture, cost/quota\naccounting, API versioning) — locking these at plan time removes a long\ntail of decisions that would otherwise surface as mid-run escalations.\n\n## 4. The integration/acceptance layer — designed at plan time\n\nThe historical failure of otherwise-good plans: every card unit-scoped,\nevery seam deferred to "the next card", no card the next card — the\nproduct ships as solid bricks with no mortar. Bake the cure into §3\n**before** any card exists:\n\n1. **Enumerate the production seams** (entrypoint, wiring/DI layer,\n   process boundaries, external services) from the §1 architecture.\n2. **One integration card per seam**, in the epics table, with an\n   **end-to-end/environmental DoD** ("a real session produces real\n   artifacts on disk"), depending on the seam\'s unit cards.\n3. **One final acceptance milestone card**, last in the DAG, depending\n   on everything: run the real build, execute a committed smoke\n   checklist, launch and click through the primary flows. **The smoke\n   checklist is authored at plan time** (a committed `docs/SMOKE.md` or\n   a §4 subsection the milestone card cites) so the milestone DoD has a\n   concrete artifact to execute.\n4. **The no-fake convention**: state in the plan that interim\n   implementations follow the canonical naming + grep-gate convention\n   (markers such as fake/silent/interim/stub in identifiers, owned by\n   the board\'s card-anatomy convention), so implementers name fakes\n   findably and the acceptance card can run the gate.\n5. **Wire-or-file at the row level**: any card row whose out-of-scope\n   defers seam wiring must name the integration card that owns that\n   wiring — the plan-level form of the wire-or-file clause.\n\nThe plan declares *which* integration cards exist and their DoDs;\ncard-level formatting of all three card kinds is the board setup\nprocess\'s job.\n\n## 5. Citation discipline\n\nCards cite the docs by stable anchor: `docs/PLAN.md §N` and\n`docs/RESEARCH.md §N` (or `:line` for point references).\n\n- Number main sections explicitly and never renumber casually —\n  sections are citation targets. New sections append.\n- Before any run launches (and at the end of authoring): **verify every\n  anchor resolves** — enumerate every `§N`/`file:line` reference in the\n  plan (card rows AND the Locked Decisions "Why" column) and check each\n  against the committed docs. A dangling reference poisons every\n  downstream stage that reads the citing card; treat it as\n  launch-blocking.\n\n## 6. Locked-decision conflicts — escalate, never silently re-decide\n\nWhen an input (a new brief, a stakeholder ask, your own research)\ncontradicts an existing Locked Decisions row: **stop and surface it\nbefore writing any plan content** — quote the locked row and its\nrationale, the conflicting requirement, and the options (re-open the\ndecision with new research, or adjust the ask). Never rewrite the row to\nmatch the new input, and never deliver a plan that quietly violates a\nstill-standing locked row. This is the same contract the runner is held\nto; the plan author doesn\'t get a bypass.\n\n## 7. Commit and wire up\n\n- Commit the resolved doc set to the target repo at the resolved paths —\n  versioned, diffable, citable. They do not live on the board.\n- Record the paths in the board definition\'s `references`\n  (`update_definition` — list fields replace whole and need re-read\n  verification) so every future session finds them.\n- Hand off to card authoring: whoever authors cards from the plan must\n  verify the cited files exist in the repo first.\n\n## 8. Review mode — auditing an existing plan\n\nWhen asked to audit rather than author, walk this checklist and output a\n**gap list** (gap → severity → location in the plan → concrete fix), not\na rewrite:\n\n| check | failing looks like |\n|---|---|\n| Locked Decisions present, each with rationale + research citation | bare choices, no why |\n| Every card row has a test-first DoD | "implement X" rows |\n| Integration layer present (seam cards + milestone + end-to-end DoDs) | all-unit DoDs; nothing forces a build — **top gap when absent** |\n| Citations resolve | dangling `§N` / out-of-range lines |\n| Architecture/state spine exists for lifecycle products | cards with no spine to anchor to |\n| Risks carried, not buried | risks only in the research doc |\n| Board-definition constraints respected | plan contradicts a definition exclusion |\n\nSeverity: the missing integration/acceptance layer and dangling\ncitations are launch-blocking; the rest are fix-before-card-authoring.\n',
        ),
        CatalogFile(
            path='references/doc-structures.md',
            content='# Grounding-doc skeletons\n\nTwo shapes distilled from real production specs: shape A is run-proven\n(an autonomous delivery executed against it); shape B is distilled from a\nreal delivered service spec. Both are deliberately tightened beyond their\nexemplars — verdict columns, per-row citations, and per-row likely-files/\nout-of-scope are stricter than the originals, so card authoring needs no\ninvention. Pick by product type; both keep the invariants: locked\ndecisions with rationale, stable numbered sections, card-ready breakdown,\nintegration/acceptance layer.\n\n## A. The two-doc split (default — products with a runtime lifecycle)\n\n### docs/RESEARCH.md\n\n```markdown\n# <Project> — Research Findings\n\n## 1. <Subsystem / decision area>\n### Strategy comparison\n| Option | <criterion> | <criterion> | <criterion> | Verdict |\n|---|---|---|---|---|\n| <candidate A> | … | … | … | recommended v1 |\n| <candidate B> | … | … | … | viable, deferred |\n| <candidate C> | … | … | … | dead end — <why> |\n### Recommended approach (v1)\n<plain statement; this feeds PLAN.md §0>\n### Reusable / reference projects\n| Project | License | Takeaway |\n|---|---|---|\n### Risks / open questions\n- …\n\n## 2. <next subsystem> …   (same shape per section)\n\n## Master source list\n<flat list of everything cited>\n```\n\nLicense hygiene: copyleft references (GPL/AGPL) marked study-only where\nthe project\'s licensing posture requires it.\n\n### docs/PLAN.md\n\n```markdown\n# <Project> — Plan\n\n## 0. Locked Decisions\n<!-- A conflict with a locked row is an escalation, never a silent rewrite. -->\n| Area | Decision | Why (see RESEARCH.md) |\n|---|---|---|\n| <area> | <choice> | <one-line rationale, §N citation> |\n\n## 1. Target Architecture\n<component diagram (ASCII is fine)>\n### State machine (the spine)        <!-- for lifecycle products -->\n<states + transitions>\n\n## 2. Repo Layout\n<tree with one-line notes>\n\n## 3. Phased Roadmap → Epics & Cards\n### Epic A — <name> (do first)\n- A1 — <goal>. DoD: write <test> covering <behavior>; implement until\n  green. Likely files: <paths>. Out of scope: <non-goals>.\n- A2 — …\n### Epic B — <name>\n…\n### Integration cards                <!-- one per production seam -->\n- INT-<seam> — <wire and prove the seam end-to-end>. DoD: end-to-end /\n  environmental ("a real <input> produces real <artifacts>"), not unit.\n  Depends on: <the seam\'s unit cards>.\n### MILESTONE — Build, smoke, accept  <!-- exactly one, last in the DAG -->\n- Run the real build; execute the committed smoke checklist; launch and\n  click through every primary flow; no-fake grep gate passes.\n\n## 4. Sequencing & Milestones\n| Gate | Means | Epics |\n|---|---|---|\n| M1 | "<checkable outcome>" | A+B |\n\n## 5. Top Risks (carry on every board)\n- …\n```\n\n## B. The consolidated spec (service/API products)\n\nOne `SPEC.md` when the product is a service whose decisions are mostly\noperational. Same invariants, plus an **operational decisions** block\nthat locks the long tail upfront:\n\n```markdown\n# <Service> — Specification\n## 1. Scope and positioning\n## 2. Core concepts            <!-- entity hierarchy, one subsection each -->\n## 3. Processing pipeline      <!-- state machine of the data flow -->\n## 4. Engine internals         <!-- interfaces, adapters, thresholds -->\n## 5. Lifecycle & management\n## 6. Versioning\n## 7. API surface (v1)         <!-- endpoint tables per resource -->\n## 8. Non-functional requirements   <!-- cost/quotas, observability: first-class -->\n## 9. Tech stack (locked for v1)\n### 9.1 Repository layout\n## 10. Out of scope (explicitly)\n### 10.1 Deferred but structurally prepared\n## 11. Resolved positions (v1)      <!-- + "Still open (non-blocking)" -->\n## 12. Operational decisions (locked for v1)\n### auth · identifiers · ingestion · failure handling · validation ·\n### webhooks/delivery contract · config & secrets · deployment posture ·\n### pricing/cost model · API versioning · audit log\n## 13. Suggested build order        <!-- sequential steps; cards derive from it -->\n```\n\nEven in this shape, the build-order section must carry the integration/\nacceptance layer (seam steps with end-to-end DoDs + a final\nbuild-and-smoke step) and per-step test-first DoDs — a consolidated spec\nis not exempt.\n\n## Choosing\n\n| signal | shape |\n|---|---|\n| desktop/mobile/product with a runtime lifecycle, big technology choices | two-doc split (A) |\n| service/API where operational decisions dominate | consolidated spec (B) |\n| either way | locked decisions, numbered sections, card-ready rows, integration layer |\n',
        ),
    ),
)
