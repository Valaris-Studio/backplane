# Copyright (c) 2026 Valaris Studio
# SPDX-License-Identifier: AGPL-3.0-or-later

"""Catalog entry: Agentic Board Setup (generated from the verified source bundle;
do not hand-edit the embedded content)."""

from app.services.skills.catalog_types import CatalogEntry, CatalogFile

ENTRY = CatalogEntry(
    catalog_id='agentic-board-setup',
    catalog_version=3,
    name='Agentic Board Setup',
    description='Prepares a Backplane board for an autonomous runner run — pipeline parity, typed columns, repo and agent binding, dependency DAG, integration cards, pre-launch checklist; use when setting up, validating, or first-launching a runner board.',
    files=(
        CatalogFile(
            path='SKILL.md',
            content='---\nname: Agentic Board Setup\ndescription: Prepares a Backplane board for an autonomous runner run — pipeline parity, typed columns, repo and agent binding, dependency DAG, integration cards, pre-launch checklist; use when setting up, validating, or first-launching a runner board.\ntoolsets: [start-here, work-management, knowledge-content, collaboration, autonomous-operations]\n---\n\n# Agentic Board Setup\n\nConfigure a Backplane workspace + board so a multi-role runner can deliver a\nproject autonomously **and the result is a working product**, not just merged\nPRs. The portable **procedure** lives here; the **project specifics** (repo,\nspec docs, budgets) come from the project\'s docs and the workspace itself —\nnever hardcode them.\n\n## The two governing truths (design lens — read first)\n\n1. **Make every no-op IDLE.** The dominant production failure class: a stage\n   reaches a clean exit *without consuming a card or producing a real\n   artifact*, and the system treats that as work (re-loop), as failure\n   (re-reserve), or as nothing (dead-end). Money-loops, phantom cards, poll\n   storms, label wedges, discover dead-zones — all instances. For every signal\n   your board produces (label, note, decision, fix card), ask: **who consumes\n   this, and what happens if nobody does?**\n\n2. **Unit-level Definition-of-Done ≠ working product.** A board can land\n   30/30 cards, every PR merged and unit-green, and the product cannot perform\n   its north-star function: fakes wired in the prod path, signals pumped\n   nowhere, a binary never built, cross-card UI traps no card\'s tests could\n   see. Card authoring must make **integration a first-class, card-owned,\n   dependency-gated deliverable** (step 5).\n\n## Covers / does not cover\n\n**Covers:** workspace pipeline-config parity, board + typed columns, git repo\nbinding, team + agent binding, card authoring (anatomy, labels, DAG,\nintegration layer), pre-launch validation, launch, and the handoff to live\nmonitoring (companion skill: Autonomous Run Monitoring).\n\n**Does not cover:** building or deploying the runner binary or backend;\nauthoring pipeline stage prompts from scratch (you verify stored prompt rows\nexist); designing a new pipeline config (you bring a workspace to parity with\nthe known-good reference via portable bundles); infrastructure. The product\nspec (PLAN/RESEARCH docs) is an input, not part of this skill.\n\n## Inputs to resolve first\n\n- **Workspace** — existing or new. New workspaces seed the 6-role default\n  pipeline; existing ones need the parity check (step 1).\n- **Git repo** — URL, default branch, integration branch (null ⇒ default).\n- **Spec grounding** — the repo\'s committed plan/research docs (e.g.\n  `docs/PLAN.md` with a Locked Decisions table, `docs/RESEARCH.md`). Cards\n  cite these by section/line; verify the referenced files exist in the repo.\n  This is the single highest-leverage input to run quality.\n- **Budget rails** — per-card budget and circuit-breaker threshold; take\n  concrete amounts from the project/board definition, not from this skill.\n- **The agent** — a dedicated platform agent (never a human key) with its own\n  API key. If missing, provision with `create_agent` in the workspace; the\n  key is issued at creation — store it in the operator\'s environment, never\n  in a repo file.\n- **The reference workspace** — the workspace whose pipeline config is the\n  known-good hardened reference for parity (step 1). Resolve it at runtime\n  from your organization\'s operations board definition or pinned notes\n  (`get_definition` / `list_notes`); if it isn\'t recorded there, ask the\n  operator. Never guess.\n\n## Step 1 — Workspace pipeline parity\n\nA live board usually runs a **stored config override**; an un-hardened older\nconfig reproduces known failure classes. Before any run on an existing\nworkspace, bring it to parity with the reference config:\n\n1. `export_pipeline_bundle` from the reference workspace →\n   `import_pipeline_bundle` with `dry_run=true` → review the preview → commit\n   by re-running with `dry_run=false`. The tool takes only\n   `(workspace_slug, bundle, dry_run)` — there is **no** top-level\n   `expected_version` argument. The race guard lives INSIDE the envelope:\n   `bundle.data.expected_pipeline_version` (409 on stale when set). Exports\n   mint it null — leave it null for a cross-workspace reference→target\n   import; set it only when round-tripping back to the SAME workspace.\n   Commit is atomic, idempotent on re-import. For targeted reads/patches:\n   `get_workspace_config`, then `update_workspace_config` (passing the\n   version you read as `expected_version` — that top-level argument exists\n   only there), then re-read to verify.\n2. The bundle\'s `data.description` **is the setup contract** — the\n   machine-readable list of columns, label gates, and role orchestration the\n   config expects. Feed it to step 2; don\'t invent board shape.\n3. Must-have parity items (verify each by re-reading the config —\n   **2xx ≠ persisted**, always re-read after every config write):\n   - `context_sources` on implementer / reviewer / rework_mediator\n     (server-side note injection; without it roles fabricate their inputs),\n   - `clear_implementer_hero` before `wake_implementer` in the mediator,\n   - reviewer `request_changes` verdict carries `failure_class: needs_rework`,\n   - ui_validator checks out the **integration head**, never the stale PR\n     branch (post-merge audits the integrated result; pre-merge audits the PR),\n   - `all_dependencies_done` on planner + implementer + ui_validator,\n   - a merge-conflict `on_failure` edge on the merge step,\n   - no dead label steps nothing consumes.\n4. **Prompt wiring fact:** the runner caches **stored prompt rows** only;\n   registry defaults never reach it, and deleting a stale override falls back\n   to the runner\'s *built-in* prompt (which may lack your fix). A corrected\n   prompt must exist as a stored row. Verify rows for every active role with\n   `list_prompt_configs` / `get_prompt_config` — including that the\n   reviewer\'s row carries the marker→label conversion on APPROVE (step 4).\n   If a row is missing after the bundle import, copy it from the reference\n   workspace (`get_prompt_config` there → `create_prompt_config` here),\n   then re-verify.\n5. **Config-lever hierarchy:** prompt patch < config patch < backend deploy <\n   runner binary rebuild. Prefer the smallest lever — but a new enum value\n   must be known by the *deployed backend validator* before any config using\n   it can save (422 until then), and runner-executed behavior needs the\n   *running binary* to support it.\n\nFull role-by-role reference: `references/pipeline-reference.md`.\n\n## Step 2 — Board + typed columns\n\n**`column_type` IS the scheduler.** Discover filters key on column types, not\nnames. Canonical set: `backlog`, `active`, `review`, `done` (optionally\n`blocked`). Who scans what:\n\n| column_type | scanned by |\n|---|---|\n| `backlog` | planner only |\n| `active`  | implementer, rework_mediator |\n| `review`  | reviewer |\n| `done`    | documentator, ui_validator |\n\nRules:\n- A card in a column **no configured role scans is invisible forever** (the\n  "discover dead-zone"). An **untyped column** is excluded from all role\n  scanning. `blocked`, if added, is human-managed by design: no role scans\n  it, so a human owns moving cards out of it.\n- Don\'t invent extra typed columns without checking every role\'s\n  `column_type` / `column_type_exclude` against the setup contract.\n- Create with `create_board` / `create_column`; verify types by re-reading.\n\n## Step 3 — Git repo + team + agent binding\n\n**Repo** (`create_git_repo`): bind exactly **one repo per board** — URL,\n`default_branch`, `integration_branch`. Build roles require a repo; no repo,\nno pickup. Know the gate: the implementer\'s `repo_has_no_open_pr` precondition\nis **repo-wide** — a single orphaned open PR blocks every implementer pickup\non the board. The inverse rule also holds: **one board per repo** — never\nbind a repo another active board already uses, or the boards cross-block each\nother\'s implementers through that same gate. Never leave PRs open\nout-of-band. Keep `require_branch_protection: false` on the repo binding,\nand `review_mode: "platform"` in the runner\'s own config file (a runner\nYAML key, not a repo field). Autonomy is the default: reviewer APPROVE →\nsquash-merge immediately; there is no merge-trigger knob — don\'t cargo-cult\none in.\n\n**Team + agent** (the #1 startup footgun): an agent binds to its workspace\nand pipeline config **via its TEAM**, not via `allowed_workspaces`\n(auth-scope only). No team ⇒ the agent\'s config endpoint returns empty ⇒ the\nrunner refuses to start. Fix shape: `create_team` in the workspace, then\n`add_team_member` for the agent with **roles = every active pipeline role** —\nthe member\'s `roles` list IS the agent\'s team_roles (there is no team-level\nroles field; re-adding a member REPLACES its roles, so always send the full\nunion). team_roles is the runner\'s claimed-role set: **a pipeline role absent\nfrom team_roles silently never runs** — no error, the stage just never fires.\nVerify by re-read — `list_teams` must show the team, `get_team` must show the\nagent among its members with the full role set — never from the write\'s 2xx.\nTeams are API/MCP-only (no UI). Identity quirk: card participation is\nattributed to the agent\'s *owning user* — expected, don\'t "fix" it.\n\n## Step 4 — Card authoring\n\nUse the full card structure in `references/card-anatomy.md`. The anatomy that\nearns its keep: **Context** (pointers into the repo\'s committed spec docs —\nverify they exist), **Goal** (one sentence), **Scope** (concrete\ndeliverables), **Acceptance Criteria** (observable outcomes the reviewer\nreviews against), **Test-First DoD** (the pipeline is TDD-mandatory; an\nuntestable DoD produces untestable diffs), **Likely Files** (cuts exploration\ncost dramatically), **Out of Scope** (explicit non-goals — exactly where\nintegration wiring silently leaks out; see step 5), **Depends On**\n(human-readable mirror of the machine edges). Plus `priority` (scheduler is\npriority-mode), `card_type`, and organizational epic/milestone labels.\n\n**Labels are ROUTING, not decoration.** The default flow: `planned`,\n`planning-failed`, `direct-implement`, `needs-ui-validation`, `ui-validated`,\n`rework-mediation-failed`, `documented`, `duplicate`. The wedge rules:\n\n1. **Never pre-seed a late-stage routing label.** A label that gates a LATE\n   stage applied at an EARLY stage strands the card (every build role\n   excludes it; the gated stage never scans that column). For UI validation\n   use the **body marker** `Validation: requires-ui-validation` in the card\n   description; the reviewer applies the label on APPROVE. Omitting the\n   marker on UI cards means the validator stays dormant the whole run — put\n   it on **every card with a real UI surface**.\n2. `direct-implement` (+ `planned`) only on cards that should skip planning —\n   and create (or move) those cards in the **active** column: in backlog the\n   planner excludes `planned` and the implementer never scans backlog, so a\n   direct-implement card in backlog is dead-zoned.\n3. If an engine action owns labels+edges (e.g. fix-card creation), do **not**\n   give that role raw `create_card` — it will self-file with wrong labels and\n   no edges. Tool allowlists are part of the routing design.\n\n## Step 5 — The integration layer (what makes the product actually work)\n\nBake these four rules into the card set — the cure for "30/30 cards merged,\nproduct doesn\'t function":\n\n1. **Wire-or-file.** Any card that introduces or leaves an interim/fake/stub\n   at a seam the running app uses (main entrypoint, wiring/DI layer,\n   constructor sites) must either (a) wire the real implementation into the\n   running app\'s construction path, or (b) create the follow-up integration\n   card **on the board, with a dependency edge**, before closing. A code\n   comment is not a tracker. "Real impl exists, tests-only call sites" is\n   **not done** at board level.\n2. **Explicit integration cards with E2E DoD.** Per subsystem seam, a card\n   whose DoD is end-to-end and environmental, not unit ("a real session\n   produces real artifacts on disk", not "supervisor tests pass"). They\n   depend on the unit cards and are depended on by the milestone, so\n   `all_dependencies_done` enforces ordering automatically.\n3. **A final acceptance milestone card**, last in the DAG, depending on\n   everything: build the real artifact (the build command actually runs),\n   execute a smoke checklist against the real build, and **launch the app and\n   click through it**. Cross-card UI bugs need an E2E smoke, not unit tests —\n   both halves of such a bug are individually green.\n4. **Grep-able no-fake assertions.** Name fakes consistently with the\n   canonical markers `fake`, `silent`, `interim`, `stub` (any casing) and\n   have the acceptance card (or CI) run a **case-insensitive** grep\n   (`grep -rni \'fake\\|silent\\|interim\\|stub\'`) over the prod construction\n   paths, requiring zero hits or a linked card per hit. Symmetric check:\n   every "real impl" must have ≥1 production call site outside tests. Cheap,\n   mechanical, catches the whole class at review time.\n\nReady-to-copy integration + milestone card blueprints:\n`references/card-anatomy.md`.\n\n## Step 6 — Dependency DAG\n\n- Create edges with `add_card_dependency` / `bulk_set_card_dependencies`;\n  the edges are the machine truth, the "Depends On" text is commentary.\n- Validate with `validate_board_dependencies` (no cycles) and spot-check with\n  `get_card_dependency_status`.\n- **The auditor invariant:** any role or human that files follow-up/fix work\n  MUST add an edge making that work block the audited item\n  (source depends_on fix). Without it the auditor re-audits the unfixed\n  source every poll — a money-loop — while the fixes starve.\n- Compute and **record the expected cycle-1 eligible set** (cards with no\n  unmet dependencies in backlog) before launch — your first live correctness\n  check.\n\n## Step 7 — Pre-launch checklist + launch\n\nWalk `references/launch-checklist.md` in order. Summary:\n\n1. Workspace parity verified by re-read (step 1) — prompt rows included.\n2. Board: typed columns; full-anatomy cards incl. integration + milestone;\n   DAG validated; cycle-1 set recorded; **zero pre-seeded routing labels**;\n   UI cards carry the body marker; repo bound; spec docs exist in the repo.\n3. Agent + team: dedicated agent key; team has the agent and ALL active roles\n   in team_roles; the agent\'s config endpoint returns the pipeline config.\n4. Runner config file (git-ignored; keys via env): workspace, board ids,\n   per-card budget high enough to never interrupt mid-card, MCP config path\n   carrying the agent key, context dirs for headless skill discovery.\n5. Binary carries every runner-side fix you depend on (config-only fixes\n   flow live; runner-executed behavior needs the binary).\n6. Dry validation: run the runner\'s discover-only mode — proves config load,\n   identity, deny-floor, MCP wiring, without reserving cards.\n7. Cost rails: circuit-breaker threshold above per-card budget, low enough\n   to catch loops; verify the binary also has the $0-failure (empty-LLM)\n   breaker — the cost breaker is blind to zero-cost spins.\n8. Launch with a tee\'d log and a **written watch-list** (cycle-1 set;\n   injected context rendered; no same-card re-reservation after approve; no\n   repeated no-diff implements; integration-head checkout on post-merge\n   validation; **pending approvals** — sweep `list_approvals(status="pending")`\n   and resolve with `decide_approval` regularly; an undecided approval looks\n   like an unexplained stall). `list_executions` is the mechanical read —\n   filter by `role` / `card_id`, keep `limit` small (rows are heavy); before\n   launching on a previously-run workspace, `list_executions(status="inflight")`\n   must come back EMPTY (cancel stale zombies with `cancel_execution`).\n\nThen switch to the companion **Autonomous Run Monitoring** skill for live\nsignal reading, kill-vs-wait calls, and cleanup recipes.\n\n## Design heuristics (apply to ANY customization)\n\nEvery produced signal needs a named consumer. Every approval maps to a\nterminal action (close/proceed/block — never "lets the next pass run"). Every\nauditor\'s spawned work blocks the audited item. Every fail path ends\nsomewhere some role can discover (audit custom fail paths against the step-2\nmatrix; restore the column BEFORE unassigning so a transient error can\'t\nstrand the card). Every no-op is IDLE. Every breaker has a $0-blind-spot\ncheck. Every label gate is checked against "can the card still reach the\nstage that needs it." Every seam fake has a card with an edge.\n',
        ),
        CatalogFile(
            path='references/card-anatomy.md',
            content='# Card anatomy — body structure, labels, integration layer, dependency edges\n\nDeep reference for steps 4–6 of Agentic Board Setup. Distilled from\nproduction runs; the anatomy below was judged "plan-grade, no restructure\nneeded" by a post-run audit — copy it, don\'t improvise.\n\n---\n\n## 1. The card body\n\nEvery section earns its place; omitting one has a known failure mode. The\none-line *why* notes are for you, the author — strip them from real cards.\n\n### Ready-to-copy skeleton\n\n```markdown\n## Context\n<!-- why: points at the repo\'s committed spec docs — durable, version-controlled\n     grounding the stage prompts can\'t drift from. Verify every referenced file\n     exists in the repo BEFORE launch; dangling refs poison every downstream stage. -->\nWhere this fits in the system. References: docs/PLAN.md §N, docs/RESEARCH.md:LINE.\n\n## Goal\n<!-- why: one sentence of intent — the planner\'s plan note and the reviewer\'s\n     judgment both anchor on it. -->\nOne sentence.\n\n## Scope\n<!-- why: concrete deliverables keep the implementer from gold-plating and give\n     the reviewer a checklist. -->\n- Deliverable 1\n- Deliverable 2\n\n## Acceptance Criteria\n<!-- why: observable outcomes — this is what the reviewer reviews against. -->\n- [ ] Observable outcome 1\n- [ ] Observable outcome 2\n\n## Test-First DoD\n<!-- why: the pipeline is TDD-mandatory; a card without a testable DoD produces\n     untestable diffs. -->\nWrite <test> covering <behavior>; implement until green.\n\n## Likely Files\n<!-- why: cuts exploration cost dramatically (a major share of per-card spend)\n     and anchors the plan note. -->\n- path/to/file.go\n- path/to/other_test.go\n\n## Out of Scope\n<!-- why: explicit non-goals prevent cross-card collisions. WARNING: this is\n     exactly where main-layer wiring silently leaks out of every card — apply\n     the wire-or-file clause (§4) whenever you write "wiring lands in a later\n     card" here. -->\n- Non-goal 1 (handled by card X)\n\n## Depends On\n<!-- why: human-readable mirror of the dependency edges. The EDGES are the\n     machine truth (§5); this text only helps the planner reason. -->\n- CARD-A (provides the interface this consumes)\n```\n\nCard metadata (set alongside the body):\n\n| field | rule |\n|---|---|\n| `priority` | scheduler runs in priority mode — urgent fix cards preempt |\n| `card_type` | `feature` / `bug` / `task` |\n| epic/milestone labels | e.g. `M1`, `M2` — organizational only, never routing |\n| body marker | `Validation: requires-ui-validation` on every card with a real UI surface (§2) |\n\nWhen filling the skeleton, keep Context citations concrete\n("docs/PLAN.md §3, Locked Decisions row 4", "docs/RESEARCH.md:142") and end\nevery seam-touching card with the wire-or-file DoD clause (§4) verbatim.\n\n---\n\n## 2. Routing labels\n\n**Labels are ROUTING, not decoration.** Each one is read by a discover\nfilter; misplacing one changes which roles can see the card.\n\n| label | applied by | when | consumed by |\n|---|---|---|---|\n| `planned` | planner (success path) | after the plan note, before moving to active | planner `exclude_label` (no re-pickup); implementer takes the card in active |\n| `planning-failed` | planner (fail path) | plan stage failed | nobody — telemetry only; NOT a discover-exclude, the planner re-picks the card |\n| `direct-implement` | card author or the engine fix-card action — always together with `planned` | at creation, ONLY for cards that must skip planning (engine fix cards land in active with `[frontend, ui-fix, direct-implement, planned]`) | planner `exclude_label` (skips planning) |\n| `needs-ui-validation` | **reviewer, on the APPROVE path** — never the author | when the card body carries the marker `Validation: requires-ui-validation` | ui_validator `include_label` on done; also `exclude_label` on every build role |\n| `ui-validated` | ui_validator (approve path) | after the validation note; `needs-ui-validation` removed at the same time | nobody — terminal marker |\n| `rework-mediation-failed` | rework_mediator (fail path) | mediation stage failed | nobody — telemetry only |\n| `documented` | documentator | after documentation (role disabled in the default pipeline) | documentator `exclude_label` |\n| `duplicate` | approval-gated close-as-duplicate flow | implementer declares `resolution: duplicate` and a human approves the terminal close | nobody — terminal marker. "Closed because already shipped" ≠ "built here" |\n\n### The body-marker mechanism\n\nThe ui_validator is the pipeline\'s only "actually look at it" stage and it is\n**dormant by default**: it fires only on done-column cards carrying\n`needs-ui-validation`. The sanctioned activation path:\n\n1. The author puts the machine-readable line\n   `Validation: requires-ui-validation` in the card description — on **every\n   card with a real UI surface**.\n2. The reviewer\'s prompt converts marker → label on the APPROVE path.\n3. The ui_validator then discovers the card in done.\n\nOmitting the marker means zero UI validation for the whole run — this is how\na cross-card UI trap (each half individually unit-green) shipped in a\nproduction run.\n\n**Surface-type resolution** — what the ui_validator can actually reach:\n\n- **(A) Web app** — validated in a browser at the app\'s base URL.\n- **(B) Desktop/webview app** — only the frontend dev server can be validated\n  standalone, with the native bridge faithfully mocked. The PACKAGED app is\n  out of the validator\'s scope: a desktop webview `.app` cannot be\n  browser-tested, and its webview is not Chromium.\n- **(C) VM-renderable surfaces** — validated via VM tooling.\n\nConsequence: for desktop/webview products, the packaged-app click-through\nMUST live in the acceptance milestone card\'s smoke checklist (§3b) — no\npipeline stage covers it.\n\n### The never-pre-seed rule (wedge mechanics)\n\n**A routing label that gates a LATE stage must never be applied at an EARLY\nstage.** The wedge, from a known incident: `needs-ui-validation` pre-seeded\non backlog cards. Every build role carries it as an `exclude_label`, and the\nui_validator only scans the **done** column. Chicken-and-egg: no role can\nbuild the card, so it can never reach done, so the only stage that wants the\nlabel can never see it. The card is stranded until a human strips the label.\n\nCorollaries:\n\n- Pre-launch checklist item: **zero pre-seeded routing labels** on the board\n  (the body marker carries the intent instead).\n- If an engine action owns a label+edge combination (e.g. fix-card creation),\n  do NOT also give that role\'s LLM raw `create_card` — it will self-file\n  cards with wrong labels and no edges. Tool allowlists are part of the\n  routing design.\n\n---\n\n## 3. Integration-layer card blueprints\n\nThree card kinds cure "every card merged, product doesn\'t function." They\nare ordinary cards — same anatomy as §1 — with E2E DoDs and a fixed place in\nthe DAG.\n\n### 3a. Seam integration card\n\nOne per subsystem seam. Its DoD is **end-to-end and environmental, not\nunit**: real environment, real signal, real artifacts — never "the package\'s\ntests pass." It depends on the seam\'s unit cards and is depended on by the\nmilestone (§3b), so `all_dependencies_done` enforces ordering automatically.\n\n```markdown\n# INT-CAPTURE — Wire and prove the capture seam end-to-end\n\n## Goal\nA real session, started through the running app, produces real capture\nartifacts on disk.\n\n## Scope\n- Replace every interim/fake at the capture construction site (wiring/DI\n  layer, main entrypoint) with the real implementations from B1–B5\n- Pump the seam\'s signals into their consumers (nothing constructed-and-discarded)\n\n## Acceptance Criteria  (E2E / environmental — not unit)\n- [ ] Launching the built app and starting a real session produces segment\n      files on disk; finalize yields a verified master artifact\n- [ ] The seam\'s runtime signals demonstrably reach their consumers\n- [ ] Zero interim/fake constructors remain in the prod construction path\n      (run the no-fake grep, §3c)\n\n## Test-First DoD\nWrite the environmental check first (a script or documented manual procedure\nthat exercises the real path and asserts on real artifacts), then wire until\nit passes. Unit-green is necessary, not sufficient.\n\n## Depends On\n- B1, B2, B3, B4, B5 (the seam\'s unit cards)\n```\n\n### 3b. Final acceptance milestone card\n\nExactly one, **last in the DAG, depending on everything**. Nothing else\nforces a build to ever happen: a production run merged ~30 PRs and the app\nbundle directory was empty, because no card\'s DoD was "the artifact exists."\n\n```markdown\n# MILESTONE — Build, smoke, and accept the product\n\n## Goal\nA built artifact, launched and clicked through, performs the product\'s\nnorth-star function end-to-end.\n\n## Scope\n- Run the real build command and verify the artifact exists where the\n  platform expects it (the build actually runs — never assume it)\n- Execute the committed smoke checklist (build/SMOKE.md-style): real\n  hardware, real permissions, real end-to-end scenario\n- Launch the built app and click through every primary flow\n\n## Acceptance Criteria\n- [ ] The build command exits clean and the artifact is present and launchable\n- [ ] Every item on the committed smoke checklist passes against the REAL build\n- [ ] Click-through of every primary UI flow completes without trapping the\n      user (cross-card UI bugs only surface here — an E2E smoke, NOT unit\n      tests: both halves of such a bug are individually green)\n- [ ] No-fake assertion block passes (§3c)\n\n## Test-First DoD\nThe smoke checklist IS the test: commit it to the repo first, then satisfy it.\n\n## Out of Scope\n- Fixing what the smoke finds beyond small wiring patches — file fix cards on\n  the board WITH dependency edges back to this card (auditor invariant, §5)\n\n## Depends On\n- Every other card on the board (this card is the DAG\'s sink)\n```\n\n### 3c. Grep-able no-fake assertion block\n\nEmbed verbatim in the acceptance card (and/or a seam integration card, a\nreviewer directive, or a repo CI check). Cheap, mechanical — it catches the\nentire "fake left in the prod path" class at review time.\n\n```markdown\n## No-fake-in-prod assertions\nConvention: every interim implementation carries a canonical marker in its\nname — `fake`, `silent`, `interim`, or `stub` — in ANY casing.\n\n1. Zero fakes in prod construction paths (case-insensitive; run over the\n   repo\'s entrypoint, wiring/DI layer, and constructor sites):\n\n       grep -rni \'fake\\|silent\\|interim\\|stub\' <entrypoint> <wiring/DI layer> <constructor sites>\n\n   Required result: **zero hits** in production construction paths — or one\n   explicit linked card per hit, with a dependency edge.\n\n2. Symmetric check — every "real impl" has ≥1 production call site: for each\n   real constructor, grep its call sites and require at least one outside\n   test files. ("Real impl exists, tests-only call sites" = not done.)\n```\n\n---\n\n## 4. The wire-or-file rule (pasteable DoD clause)\n\nPaste into the DoD of **every card that touches a production seam** (main\nentrypoint, the wiring/DI layer, constructor sites the running app uses):\n\n```markdown\n**Wire-or-file (DoD clause):** If this card introduces or leaves an\ninterim/fake/stub at a seam the running app uses, it is NOT done until ONE of:\n(a) the real implementation is wired into the running app\'s construction path,\nOR (b) a follow-up integration card exists ON THE BOARD with a dependency edge,\ncreated before this card closes. A code comment is not a tracker. "Real impl\nexists, tests-only call sites" is NOT done at board level.\n```\n\nWhy it exists: in a production run, seams were stubbed with self-documented\ninterim fakes whose comments deferred wiring to "the next card" — **and no\ncard was the next card**. The result shipped: a fake capturer in the prod\npath, a recognizer returning always-empty output, signals never pumped into\nthe orchestrator, and a binary never built. "Out of Scope: wiring" is\nlegitimate **only** when paired with option (b).\n\n---\n\n## 5. Dependency edges — the DAG is load-bearing\n\n- **Edges are the machine truth; the "Depends On" body text is commentary.**\n  Create with `add_card_dependency` / `bulk_set_card_dependencies`; validate\n  the whole board with `validate_board_dependencies` (no cycles); spot-check\n  individual cards with `get_card_dependency_status`.\n- `all_dependencies_done` on the planner, implementer, and ui_validator\n  discover filters means **a card is untouchable until every dependency card\n  is Done** — this makes the integration layer self-sequencing: unit cards →\n  seam integration cards → the milestone.\n- **The auditor invariant:** any role or human that files follow-up/fix work\n  MUST add an edge making that work block the audited item\n  (`source depends_on fix_card`). The engine fix-card action adds this edge\n  automatically; **if you ever hand-file fix cards, add the edges yourself.**\n  Without the edge, the auditor re-audits the unfixed source on every poll —\n  a recurring-cost money-loop — while the actual fixes starve.\n- **Cycle-1 eligible set:** before launch, compute the set of backlog cards\n  with zero unmet dependencies — the only cards the planner may reserve on\n  the first cycle. Record the set in your launch watch-list; the first live\n  poll either matches it (correct) or doesn\'t (stop and debug the DAG before\n  anything else runs).\n',
        ),
        CatalogFile(
            path='references/launch-checklist.md',
            content='# Pre-launch checklist\n\nWalk top to bottom before starting the runner. Every item is a real failure\nmode from production runs. Do not launch with an unchecked box — the items\nare ordered so that earlier failures invalidate later checks.\n\nDiscipline for EVERY config check below: **2xx ≠ persisted.** After any\nwrite, independently re-read the entity (version bump + the specific fields)\nbefore checking the box.\n\nCompanion discipline: **setup mutations are idempotent.** Create/add on an\nalready-existing entity returns the entity (200/201, never 409) — re-running\na failed setup step is safe, and a 200 on retry is the existing entity, not a\nduplicate. Idempotency makes retries safe; it does **not** replace re-read\nverification.\n\n## 1. Backend / workspace parity\n\n- [ ] Workspace pipeline config at parity with the reference workspace.\n      Sanctioned path: `export_pipeline_bundle` from the reference →\n      `import_pipeline_bundle` with `dry_run=true` → review the preview →\n      commit by re-running with `dry_run=false` (atomic, idempotent). The\n      concurrency guard is IN the envelope —\n      `bundle.data.expected_pipeline_version` (409 on stale when set; null\n      on export, leave null for cross-workspace imports) — NOT a tool\n      argument: a top-level `expected_version` exists only on\n      `update_workspace_config`. Resolving the reference workspace: recorded\n      in your organization\'s operations board definition or pinned notes\n      (`get_definition` / `list_notes`); if absent, ask the operator — never\n      guess.\n- [ ] All must-have config items present (verify each by re-read):\n      - `context_sources` on implementer / reviewer / rework_mediator —\n        declared on BOTH the stage `llm` block and the lifecycle llm-step\n        params (the lifecycle copy is load-bearing),\n      - `clear_implementer_hero` before `wake_implementer` in the mediator,\n      - reviewer `request_changes` verdict carries\n        `failure_class: needs_rework`,\n      - ui_validator uses `checkout_integration_head` (NOT\n        `checkout_pr_branch` — post-merge audits the integrated result),\n      - `all_dependencies_done` on planner + implementer + ui_validator,\n      - merge-conflict `on_failure` edge on the merge step,\n      - no dead label steps nothing consumes.\n- [ ] **Prompt ROWS verified, not registry defaults.** The runner caches\n      stored prompt rows only; deleting a stale override falls back to the\n      runner\'s *built-in* prompt, which may lack your fix. For every active\n      role, confirm a stored row exists (`list_prompt_configs` /\n      `get_prompt_config`) carrying the injected-context blocks (PlanNote /\n      ReviewHistory references) and no dead `list_notes` instructions —\n      INCLUDING the reviewer\'s marker → label conversion on APPROVE. If the\n      bundle import did not bring a row, copy it from the reference\n      workspace (`get_prompt_config` there → `create_prompt_config` here)\n      and re-verify.\n- [ ] Every config write in this section confirmed by independent re-read.\n\n## 2. Board checks\n\n- [ ] Columns are **typed** and match the setup contract (the bundle\'s\n      `data.description` is the machine-readable ground truth). Discover\n      filters key on `column_type`, not names; an untyped column is\n      invisible to every role.\n- [ ] Every card has the full anatomy: Context (citing the repo\'s committed\n      spec docs by section/line), Goal, Scope, Acceptance Criteria,\n      Test-First DoD, Likely Files, Out of Scope, Depends On — plus\n      priority, card_type, and organizational epic/milestone labels.\n- [ ] The card set includes the **integration layer**: explicit integration\n      cards with E2E/environmental DoD per subsystem seam, AND a final\n      acceptance **milestone card** last in the DAG (build the real\n      artifact, run the smoke checklist, launch the app and click through).\n- [ ] DAG validated with `validate_board_dependencies` (no cycles);\n      spot-check edges with `get_card_dependency_status`.\n- [ ] **Cycle-1 eligible set computed AND written down** — the cards with no\n      unmet dependencies the planner should reserve first. Your first live\n      correctness check after launch.\n- [ ] **Zero pre-seeded routing labels.** No card in backlog carries\n      `needs-ui-validation` or any other late-stage gate label (pre-seeding\n      strands the card).\n- [ ] UI body markers present: every card with a real UI surface carries\n      `Validation: requires-ui-validation` in its description (the reviewer\n      converts marker → label on APPROVE). No marker ⇒ the validator stays\n      dormant the whole run.\n- [ ] Exactly one git repo bound to the board (`list_git_repos`): URL,\n      default branch, integration branch. No open PRs on the repo — the\n      implementer\'s `repo_has_no_open_pr` precondition is **repo-wide**: one\n      orphaned open PR blocks every implementer pickup. **One board per\n      repo** — boards sharing a repo block each other\'s implementers.\n- [ ] Every spec-doc reference in card Context sections points at a file\n      that **actually exists in the repo** — dangling refs poison every\n      downstream stage.\n\n## 3. Agent + team checks\n\n- [ ] A **dedicated platform agent** with its own API key — running with a\n      human key is **forbidden**. If it does not exist, create it with\n      `create_agent` in the workspace; the API key is issued at creation —\n      store it in the operator\'s environment only, never in any repo file.\n      Card participation is attributed to the owning user; expected, don\'t\n      "fix" it.\n- [ ] A team exists in the workspace and **contains the agent**\n      (`create_team` + `add_team_member`; teams are API/MCP-only). The agent\n      binds to the workspace and pipeline config via its TEAM — not via\n      `allowed_workspaces` (auth-scope only). Verify by re-read:\n      `list_teams` shows the team, `get_team` shows the agent among its\n      members.\n- [ ] **team_roles covers ALL active pipeline roles.** A pipeline role\n      absent from team_roles silently never runs — no error, the stage just\n      never fires. Disabled roles may be omitted. Confirm the PERSISTED set\n      with `get_team` (re-adding a member REPLACES its roles — send the\n      union).\n- [ ] The agent\'s config endpoint (`get_agent_config`) returns the pipeline\n      config + team_roles + prompt_configs. An empty response means the\n      runner will refuse to start.\n\n## 4. Runner config checks\n\nThe config file is **YAML** and lives **git-ignored in the runner repo\'s\nconfigs directory**. The runner repo\'s own docs/examples are the\nauthoritative schema; the list below states which values matter.\n\n- [ ] The runner config file is **git-ignored** — keys live in it; it never\n      enters version control.\n- [ ] API key supplied via environment variable (e.g.\n      `api_key: ${BACKPLANE_API_KEY}`), never inline.\n- [ ] Production API URL, workspace slug, and board ids set.\n- [ ] **Per-card budget** (`max_budget_usd`) high enough to never interrupt\n      a card mid-work — an interrupted card is worse than an expensive one;\n      the circuit breaker (section 7) is the loop backstop, not the per-card\n      cap.\n- [ ] `mcp_config_path` points at a run-specific MCP config carrying the\n      agent key in its environment — that authenticates every MCP call the\n      LLM stages make.\n- [ ] `llm.context_dirs` includes the user-level config directory (mapped to\n      the headless CLI\'s add-dir mechanism) — the explicit trigger for\n      headless skill discovery.\n- [ ] **Strict-MCP-config caveat:** the headless runner silently ignores any\n      MCP server not listed in its MCP config. Any MCP-delivered capability\n      a stage needs must be added there explicitly. To confirm what a\n      platform-MCP session can actually see, call `get_server_info` (server\n      version, registered tools, allowlist, enabled set) — a tool that\n      "doesn\'t exist" is usually version or allowlist drift.\n- [ ] Defaults sane: `auto_pr: true`, `merge_strategy: squash`,\n      `review_mode: platform`.\n- [ ] No dead/removed keys (e.g. there is no merge-trigger knob — don\'t\n      cargo-cult one from old configs).\n\n## 5. Binary checks\n\nConfig-only fixes flow live to a running runner via platform-config refresh;\n**runner-executed behavior needs the binary swap**. Verify fix vintage with\nthese recipes:\n\n- [ ] **Build commit:** check the binary\'s build commit against the runner\n      repo log with whoever built it.\n- [ ] **Behavioral tells** — e.g. 3+ consecutive $0 empty-LLM failures\n      without an automatic ~15-min pause means the empty-LLM breaker is\n      absent.\n- [ ] **Backend-validator gates** (new enum values): probe by saving a\n      config using the value with `dry_run` — a 422 means the deployed\n      validator predates it.\n\n| needs the BINARY (runner-executed) | flows live (config/prompt/backend) |\n|---|---|\n| auto dependency edges on engine-created fix cards | discover filters, labels, column gates |\n| consecutive no-diff implement backstop | lifecycle step graphs and fail-path edges |\n| `checkout_integration_head` execution support | `context_sources` declarations |\n| empty-LLM ($0 failure) breaker | stored prompt rows |\n| self-trigger rate-floor | cost-breaker thresholds |\n\nDeploy gate in the other direction: a config using a new enum value cannot\neven be **saved** until the deployed backend validator knows it (422 until\nthen).\n\n## 6. Dry validation\n\n- [ ] Run the runner in **discover-only mode** (e.g.\n      `--config <cfg> --discover`). It exits before the work loop and\n      proves, without reserving any cards:\n      - the config file loads,\n      - the runner\'s identity is the **agent** (not a human),\n      - the deny-floor tool policy is active,\n      - the MCP config is generated/wired.\n\n## 7. Cost rails\n\n- [ ] Cost circuit breaker configured: threshold **above the per-card\n      budget** but low enough to catch loops (reference shape: per-card\n      budget slightly below the per-15-minute threshold, action `pause`).\n      Two heavy cards in one window can trip a pause a human resumes —\n      acceptable backstop.\n- [ ] **The $0 blind spot:** the cost breaker cannot see zero-cost failures.\n      Provider quota exhaustion returns instantly with exit code 1 and zero\n      tokens/cost; without the runner-side empty-LLM breaker (3 consecutive\n      empty results → pause reservations ~15 min, heartbeat kept, expiry as\n      probe) the runner re-reserves forever. Verify in section 5 that the\n      launch binary carries it.\n\n## 8. Launch\n\n- [ ] **No leftover in-flight executions.** On a workspace that has run\n      before, `list_executions` with `status="inflight"` (a virtual filter:\n      every started/running row not yet terminal, unbounded by `limit`)\n      must come back empty — cancel stale zombie rows with\n      `cancel_execution` first, or they masquerade as live work.\n- [ ] Launch with a **tee\'d log**, named per run with a timestamp, e.g.:\n      `API_KEY=… ./bin/<runner> --config <cfg> 2>&1 | tee /tmp/<run>/run-$(date +%F-%H%M).log`\n- [ ] **Write the watch-list down BEFORE launching** — six items:\n      1. The planner reserves exactly the recorded **cycle-1 eligible set**.\n      2. Injected context renders (PlanNote present in the implementer\'s\n         prompt) and there is **no** "context source declared but not\n         referenced" warning.\n      3. No same-card re-reservation minutes after a clean approve.\n      4. No repeated no-diff implements on any card.\n      5. The post-merge validator checks out the **integration head**, not\n         the PR branch.\n      6. **Pending approvals** — the implementer\'s approval gate blocks\n         until a human decides; discover with\n         `list_approvals(status="pending")`, inspect with\n         `get_approval_status`, resolve with `decide_approval`. An undecided\n         approval reads as an unexplained stall.\n\n      Items 1–4 are read mechanically with `list_executions` — filter by\n      `role` or `card_id` (card_id scopes to that card\'s pipeline history —\n      newest 200 rows), keep `limit` small: rows are heavy, carrying full\n      prompts and tool invocations.\n\nThen hand off to the companion Autonomous Run Monitoring skill (kill-vs-wait\ncalls, cleanup recipes).\n\n---\n\n## Appendix — what good grounding looks like\n\nThe single highest-leverage input to run quality is the repo\'s committed spec\ndocs; per-card cost and reversion rate between production runs differed\nlargely on spec quality. The proven shape:\n\n- **Research doc** — option matrices, risks, reusable references.\n- **Plan doc** — a **Locked Decisions table** with rationale, a target\n  architecture diagram, repo layout, phased epics broken into cards each\n  with a **test-first DoD**, sequencing / milestones, and top risks carried\n  onto the board.\n\nCards cite these docs **by section/line** in their Context section — durable,\nversion-controlled grounding the prompts can\'t drift from (verify the cited\nfiles exist; section 2 above).\n\n**Flag:** plan docs historically lack the **integration/acceptance layer** —\nwire-or-file rules, seam-level integration cards with E2E DoD, the final\nacceptance milestone, grep-able no-fake assertions. ADD that layer on top of\neven a strong plan doc; without it a plan-grade card set can land every card\nunit-green and still ship a product that cannot perform its north-star\nfunction.\n',
        ),
        CatalogFile(
            path='references/pipeline-reference.md',
            content='# Pipeline reference — the 6-role default\n\nDeep reference for step 1 of Agentic Board Setup. Describes the 6-role\ndefault pipeline as hardened in the reference workspace config —\nbattle-tested in production runs, none of it speculative.\n\n## 1. Roles and contracts\n\nScheduling: `mode: priority`, walk order **reviewer → rework_mediator →\nimplementer → planner → documentator → ui_validator** (drain downstream work\nbefore starting new work). All roles `unique: true`. Every LLM stage carries\nthe deny-floor `tool_policy.deny`: `gh pr merge/review/close`,\nforce-pushing (with or without `--with-lease`), pushing to main/master,\n`git reset --hard`.\n\n| role | discover | git | success path | fail path |\n|---|---|---|---|---|\n| **planner** | backlog scan; exclude_label `[planned, needs-ui-validation, direct-implement]`; `no_other_card_in_flight`; `all_dependencies_done`; `require_git_repo` | none | LLM `produces_note` → `create_note` (kind `plan`) → unassign self → apply `planned` → move to **active** | unassign → label `planning-failed` → end (card stays in backlog without `planned`; re-planned next poll — the label is telemetry, not a discover-exclude) |\n| **implementer** | `unassigned_or_rework` in active; exclude_label `needs-ui-validation`; `all_dependencies_done`; precondition `repo_has_no_open_pr` (**repo-wide** — one orphaned open PR blocks every implementer pickup on the board) | `create_branch` off integration_branch; `create_pr`; `force_push_on_rework` | LLM `writes_code` (model tier mid; `approval_enabled`; context_sources: PlanNote) → `create_pr` → wake reviewer → move to **review** | unassign → move back to **active**. Partial-abort wedge: a transient error between the two steps strands the card in backlog + `planned` — see §3 |\n| **reviewer** | review scan; `require_pr_url`; precondition `pr_is_open`; exclude_label `needs-ui-validation` | `checkout_pr_branch` (frozen PR branch — CORRECT pre-merge: audit the candidate) | LLM `produces_decision` (context: PlanNote). **approve** → `post_pr_review` → `merge_pr` → write `review_verdict` note → move **done** | **merge failure** → `on_failure` subtree: unassign → conflict verdict note with `failure_class: needs_rework` → wake mediator → move **active** (the merge-conflict self-heal — without it a failed merge orphans an open PR that board-blocks all implementers). **request_changes** → unassign → verdict note with `failure_class: needs_rework` → wake mediator → move **active** |\n| **rework_mediator** | active scan; `require_pr_url` + precondition `pr_is_open`; `require_note_kind: review_verdict`; **freshness gate** `require_note_kind_newer_than {review_verdict > rework_brief}` (only mediates verdicts it hasn\'t already briefed); exclude_label `needs-ui-validation` | `checkout_pr_branch` (pre-merge — correct) | LLM `produces_note` (context: review_history → `{{.ReviewHistory}}`) → write `rework_brief` note → **`clear_implementer_hero`** (`remove_card_participant` with `pipeline_role: implementer` — without it a different-agent hero blocks re-discovery) → `wake_implementer` → unassign → end. The clear MUST precede the wake | failure ends with the card still in active, unassigned → self-heals via the implementer\'s `unassigned_or_rework`; `rework-mediation-failed` label is telemetry only |\n| **documentator** | done scan; exclude_label `[documented, needs-ui-validation]` | — | **DISABLED** (`llm.enabled: false`; lifecycle = a single `end` step). Historical bug: a disabled role\'s clean no-card walk once reported "had work" and starved the role after it — fixed (clean walk with no card = IDLE), but keep disabled roles\' lifecycles minimal | n/a |\n| **ui_validator** | done scan; `include_label: needs-ui-validation`; `require_pr_url`; `all_dependencies_done` (the re-validation loop-breaker) | **`checkout_integration_head`** (fetch + checkout + hard reset to the remote default branch; hard-fail on error). NEVER `checkout_pr_branch` here — post-merge audits the integrated result; the stale PR branch re-finds already-fixed bugs and spawns phantom fix cards | LLM `produces_decision`, broad tools (it drives a real app). Surfaces: web → browser at base URL; desktop/webview → frontend dev server standalone with the native bridge mocked (packaged app is out of scope — covered only by the acceptance milestone smoke); VM-renderable → VM tooling. **approve** → `ui_validation` note → unassign → label `ui-validated` → remove `needs-ui-validation` (NO terminal move — the card is already done; validator warnings about a missing terminal move are expected) | **request_changes / on_failure** → verdict note → **`create_fix_cards`** (engine-owned kind: files fixes to active, priority urgent, type bug, labels `[frontend, ui-fix, direct-implement, planned]`, and **auto-adds the dependency edge** source-depends-on-fix) → unassign → end. NO raw `create_card` in its allowlist — an LLM with the raw tool self-files with wrong labels and no edges, and the missing edge makes it re-audit the unfixed source every poll |\n\nThe `checkout_pr_branch` / `checkout_integration_head` split is an invariant:\n**pre-merge stages audit the candidate (PR branch); post-merge stages audit\nthe integrated result (integration head).** Never overload one checkout\nstrategy for both.\n\n## 2. Discover filter semantics (precise)\n\n- `column_type` / `column_type_exclude` — column-type scoping. Column\n  *names* are irrelevant to scheduling.\n- `include_label` / `exclude_label` (string or list) — routing-label gates.\n- `skip_if_pipeline_role: X` — skip if a participant with that pipeline_role\n  already exists on the card (self-dedup).\n- `all_dependencies_done` — every dependency card is Done.\n- `require_git_repo` — board must have a bound repo.\n- `require_pr_url` — card must carry a PR URL.\n- `require_note_kind` — card must have a note of that kind.\n- `require_note_kind_newer_than {kind, than_kind}` — the freshness gate: a\n  note of `kind` must exist newer than the latest note of `than_kind`.\n- `unassigned_or_rework` — implementer-shaped: cards with no hero, or cards\n  woken for rework.\n- `no_other_card_in_flight` — planner serialization. **Post-fix semantics:\n  counts only actively-worked siblings — a card with a CardParticipant row\n  OR an unexpired AgentReservation.** A card no role can act on is NOT "in\n  flight." Pre-fix it counted any non-backlog card, and a single stranded\n  unactionable card froze ALL planning board-wide. Behavioral fix with no\n  config-level probe: confirm the deployed backend commit with whoever\n  deployed it.\n- `preconditions`:\n  - `repo_has_no_open_pr` — **REPO-wide**, not per-card: one orphaned open\n    PR blocks all implementer pickup on the board.\n  - `pr_is_open` — the card\'s PR is still open.\n\n**The split-brain trap:** every discover predicate must live in BOTH the\ncandidate-selection check AND the reservation-eligibility check in the\nbackend. When one had label predicates the other lacked, a mid-reservation\nlabel change caused per-poll re-handing of an already-approved card. When the\nplatform adds a predicate, check both.\n\n**The two-spots rule:** the discover filters appear TWICE in the pipeline\nconfig — once in the flat stage `discover` block and once in the lifecycle\n`discover` step params. Keep them identical; every patch must hit both spots\nand be verified by independent re-read (2xx ≠ persisted).\n\n## 3. Fail-path design rules\n\n- **Every `llm`, `merge_pr`, and `create_pr` step needs an `on_failure`\n  edge.** A step with a failure mode and no edge orphans state (a failed\n  merge with no `on_failure` once left an open PR that board-blocked every\n  implementer via the repo-wide gate).\n- **An empty decision soft-fails.** A `produces_decision` step returning an\n  EMPTY decision routes to `on_failure` — without that, empty verdicts\n  dead-end cards.\n- **Restore-column-before-unassign ordering.** Fail-path steps must be\n  ordered restore-first / best-effort. The implementer fail path\n  `unassign → move_back(active)` has a wedge: a transient 429/timeout\n  between the two steps strands the card in backlog + `planned` — a discover\n  dead-zone (planner excludes `planned`; implementer never scans backlog).\n  Preferred shape: column-restore BEFORE unassign, cleanup calls\n  best-effort/non-fatal. If your config predates that shape, know the manual\n  cure: move the stranded card to active; it self-heals from there.\n- **Every fail path must terminate somewhere SOME role can discover.**\n  Reviewer/mediator failures end with the card unassigned but still in a\n  column those roles (or the implementer) scan — self-heals. Planner failure\n  leaves the card in backlog without `planned` — re-planned next poll. Audit\n  any custom fail path against the column-type/role scan matrix.\n- **Every produced signal needs a consumer** (class rule). A failed merge\n  produces a `needs_rework` verdict the mediator consumes; a failed\n  validation produces dependency-linked fix cards the implementer consumes;\n  an approved close-as-duplicate produces a terminal close. If a custom\n  branch produces a note/label/decision nothing consumes, it WILL loop or\n  strand.\n\n## 4. Circuit breakers\n\n- **Cost breaker:** `cost_circuit_breaker {enabled,\n  threshold_usd_per_15min, action: alert | pause | kill_runner}`. Threshold\n  ABOVE the per-card budget but low enough to catch loops; with action\n  `pause`, two heavy cards in one window may trip a pause a human resumes —\n  acceptable backstop. Concrete amounts come from the project/board\n  definition at runtime.\n- **Empty-LLM breaker (the $0 blind spot, runner-side):** the cost breaker\n  is blind to **$0 failures**. Provider quota/credit exhaustion returns\n  instantly with `exit_code=1, tokens_in=0, tokens_out=0, cost_usd=0`, and a\n  pre-fix runner re-reserved forever (participant add/remove board loop plus\n  backend 429 storms). Post-fix: **3 consecutive empty LLM results → pause\n  reservations and refreshes ~15 minutes** (heartbeat kept; expiry acts as\n  the probe). An environment failure is not a card defect. **Requires the\n  rebuilt runner binary** — confirm the running binary\'s commit with whoever\n  built it.\n- **No-diff backstop:** `maxConsecutiveNoChangeImplements=2` force-blocks\n  any undeclared repeated no-op implement, so no role can dollar-loop\n  regardless of prompt quality. The CLEAN path is the LLM declaring\n  `resolution: "duplicate"`, routing to an **approval-gated terminal close**\n  (Done + `duplicate` label — "closed because already shipped" ≠ "built\n  here"). Pre-fix, a clean-tree implement could only mean\n  failure→re-reserve, a per-cycle money-loop.\n- **Defense-in-depth principle:** *a declared signal for the good outcome, a\n  mechanical breaker for the floor.*\n\n## 5. Approval gates\n\n`approval_enabled: true` on the implementer; the stage prompt teaches when\nto call `request_approval`:\n\n- **Close-as-duplicate claims** (category `close_as_duplicate`) — the gate\n  caught a confidently-wrong duplicate verdict live; never let a role\n  self-certify "already shipped."\n- **Bulk or destructive changes.**\n- **Conflicts with the locked spec** — escalate a perceived locked-constraint\n  conflict to a human instead of silently rewriting the spec (proven live:\n  the pipeline escalated a false conflict rather than acting on it).\n\n**Rule: every approval outcome must map to a terminal action**\n(close / proceed / block). An approval that merely "lets the next pass run"\nis decorative — the root cause of a known money-loop incident.\n\n## 6. Handoff context_sources\n\nPrompts that tell roles to fetch their critical input themselves fail when\nthe fetching tool isn\'t in the role\'s allowlist (a hard server-side gate):\nthe implementer never reads the plan, the mediator never quotes the verdict,\nand both fabricate. The fix is **server-side `context_sources`\npre-injection**. The exact shapes:\n\n- **implementer + reviewer:**\n  `{kind: card_notes, as: PlanNote, filter: {kind: plan}}` — the prompt\n  references it as `{{ index .ContextSources "PlanNote" }}`. NOT\n  `{{.PlanNote}}` — that renders empty and trips lint.\n- **rework_mediator:** `{kind: review_history}` with **NO `as:` alias** —\n  the legacy bridge fills `{{.ReviewHistory}}`; adding an alias breaks it.\n\nDeclare context_sources in **BOTH** places: the stage `llm` block AND the\nlifecycle llm-step params — the lifecycle copy is load-bearing.\n\nLaunch check: the runner WARNs `context source declared but not referenced`\nwhen the stored prompt row doesn\'t reference a declared source. The\n**absence** of that WARN is a launch watch-list item — it proves the injected\ncontext actually renders.\n\n## 7. Prompt wiring and the config-lever hierarchy\n\n**Prompt-row wiring fact:** the runner caches **stored prompt rows** from the\nprompt-configs endpoint — no synthesis. On a cache miss it falls back to the\nrunner\'s **built-in prompt**. The backend registry defaults **never reach the\nrunner**. Consequences: a corrected prompt must exist as a stored DB row\n(`create_prompt_config` / `update_prompt_config`), and **deleting a stale\noverride falls back to the built-in**, which may lack your fix entirely.\n\n**Config-lever hierarchy:** prompt patch < config patch < backend deploy <\nrunner binary rebuild. Always prefer the smallest lever, but know the gates:\n\n- A new **enum value** (e.g. `checkout_integration_head`) must be known by\n  the **deployed backend validator** before any config using it can be\n  saved — 422 until the backend deploy lands. **Probe:** attempt to save a\n  config using the new value via `import_pipeline_bundle` with\n  `dry_run: true` — a 422 means the validator predates it; a clean pass\n  means it\'s safe to save for real.\n- **Runner-executed behavior needs the running binary**: config-only fixes\n  flow live via config refresh; new step kinds, checkout strategies, and\n  breakers need the binary swap.\n- Every config write: use the race guard, then **re-read to verify\n  persistence** (version bump plus the specific fields). 2xx ≠ persisted.\n  The guard\'s spelling differs by tool: `update_workspace_config` takes a\n  top-level `expected_version`; `import_pipeline_bundle` does NOT — its\n  guard is `bundle.data.expected_pipeline_version` inside the envelope\n  (null on export; leave null for cross-workspace imports).\n',
        ),
    ),
)
