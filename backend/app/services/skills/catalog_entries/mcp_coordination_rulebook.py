# Copyright (c) 2026 Valaris Studio
# SPDX-License-Identifier: AGPL-3.0-or-later

"""Catalog entry: MCP Coordination Rulebook (generated from the verified source bundle;
do not hand-edit the embedded content)."""

from app.services.skills.catalog_types import CatalogEntry, CatalogFile

ENTRY = CatalogEntry(
    catalog_id='mcp-coordination-rulebook',
    catalog_version=5,
    name='MCP Coordination Rulebook',
    description='Day-to-day etiquette for coordinating through Backplane MCP — session startup, entity model, definition-first context, interactive card claiming, notes, dependencies, write-and-verify; use for everyday work on an existing board.',
    files=(
        CatalogFile(
            path='SKILL.md',
            content='---\nname: MCP Coordination Rulebook\ndescription: Day-to-day etiquette for coordinating through Backplane MCP — session startup, entity model, definition-first context, interactive card claiming, notes, dependencies, write-and-verify; use for everyday work on an existing board.\ntoolsets: [start-here, work-management, knowledge-content, collaboration, autonomous-operations]\n---\n\n# MCP Coordination Rulebook\n\nHow humans and interactive agents coordinate through the Backplane\nplatform correctly. The platform is the shared memory of the team: the\nboard is the durable state, the definition is the project\'s source of\ntruth for nouns, notes are the handoff medium. This skill is the\ncross-cutting etiquette — the *verbs*. Project specifics (stack,\nconventions, goals, no-goes, column flows) are *nouns* that live in each\nboard\'s definition and the project\'s own docs, fetched at runtime, never\nassumed.\n\n**Covers:** session bootstrap, the entity map and which tool reads/writes\nwhat, definition-first context resolution, the interactive card lifecycle,\nnotes/channels/dependency conventions, write-and-verify discipline, and\ntool selection (composite tools, search vs list).\n\n**Not for:** bootstrapping a new project, board, or workspace (see the\nProject Bootstrap skill — this one assumes the board exists); configuring,\nlaunching, watching, or reconciling autonomous runner/pipeline runs; or\nany board-specific lifecycle (e.g. a governance or voting flow) — those\nare nouns: read them from the board\'s columns and definition.\n\n## 1. Start every session with get_project_context\n\nThe first platform call of any session — or whenever you are dropped into\na project mid-conversation — is `get_project_context` (workspace slug +\nboard id). One call returns the definition, board summary, notes, git\nrepos, and recent activity.\n\n- The workspace slug and board id normally live in the client project\'s\n  own docs or the conversation. When you don\'t have them, resolving ids\n  with `list_workspaces` → `list_boards` is correct discovery — that is\n  not the forbidden chain below.\n- Never reconstruct the briefing itself from a chain of `list_cards` /\n  `list_notes` / `get_definition` / `list_activity` calls — the composite\n  returns all of it in one round-trip.\n- Read the definition and pinned notes from that result *before* acting\n  or asking questions.\n- Drill down afterwards with targeted calls (`search_cards` with filters,\n  `get_card`, `list_notes` scoped to a card), not broad re-listings.\n- Workspace-level orientation without a specific board: `list_workspaces`,\n  `get_workspace_summary`, `list_boards`.\n- Capability and identity are mechanical checks, not memory:\n  `get_server_info` reports the server version and the exact tool surface\n  enabled for this session (a tool that "doesn\'t exist" is usually\n  version or allowlist drift — check, and never quote a tool count from\n  memory); `whoami` returns the platform user this session acts as (id,\n  email, name). Run them at session start or the moment either question\n  comes up.\n\n## 2. The entity map\n\n```\nWorkspace ── Boards ── Columns ── Cards (participants, labels, dependencies)\n   ├─ Notes (optionally board- and card-linked)\n   ├─ Channels (contact registry)        ├─ Definition (per board)\n   ├─ Resources                          └─ Git repos (per board)\n   ├─ Members\n   └─ Teams / Agents (runner world)\n```\n\nWhat each entity is *for*:\n\n| entity | role in coordination | primary tools |\n|---|---|---|\n| Definition | the project\'s nouns: scope, goals, no-goes, stack, conventions | `get_definition` / `update_definition` |\n| Card | a unit of work moving through the board\'s lifecycle | `get_card`, `create_card`, `update_card`, `move_card` |\n| Dependency edge | machine truth about ordering/blocking | `add_card_dependency`, `validate_board_dependencies` |\n| Note | durable context: decisions, handoffs, briefs | `create_note`, `list_notes`, `get_note`, `update_note` |\n| Channel | workspace contact registry (email/slack/phone/website entries) | `create_channel`, `list_channels` |\n| Resource | shared files/links | `create_resource`, `list_resources`, `get_download_url` |\n| Activity | append-only audit trail of mutations | `list_activity` |\n\nThe full tool-by-tool map (read/write per entity, key parameters,\ncomposite tools) is in `references/tool-map.md`.\n\nTeams, agents, prompt configs, executions, the merge queue, and\n`next_assignment` belong to the autonomous runner world — see section 4\nfor the one rule about them you must know here.\n\n## 3. Definition-first: check it before you ask\n\nProject nouns — stack, conventions, goals, exclusions ("no-goes"),\nstakeholders, key decisions, coding standards — live in the board\n`definition`, not in skills, prompts, or anyone\'s memory.\n\n- **Before asking the user a project question, check the definition.** A\n  question whose answer sits in `tech_stack`, `constraints`, or\n  `coding_standards` should never reach the user.\n- **Populate as you learn.** When the user does answer a project-noun\n  question (or a decision is made in-session), offer to record it with\n  `update_definition` so no future session asks again. An empty\n  definition is a setup gap — flag it.\n- Definition fields: `scope` (free text) plus structured `content` —\n  `objectives`, `exclusions`, `milestones`, `tech_stack`, `stakeholders`,\n  `constraints`, `decisions`, `references`, `custom_fields`,\n  `coding_standards`.\n- Grouping concepts — epics, themes, workstreams — are board conventions,\n  not platform entities. Look for them as card labels, statuses, or\n  definition `custom_fields`, per that board\'s definition.\n- **Updates are a shallow merge.** Send only the fields you are changing —\n  untouched fields are preserved — but a list field you do send is\n  replaced whole: read it first and send the complete new list. Re-read\n  after writing (section 8).\n\n## 4. The interactive card lifecycle\n\n**"Claim a card" means move it.** In an interactive session:\n\n1. Read the board (`get_board` or the session\'s `get_project_context`) to\n   see *this board\'s* columns — never assume a canonical flow or guess a\n   column by name. Each column carries a `column_type` (backlog / active /\n   review / done, or untyped).\n2. Check the card is actually workable with `get_card_dependency_status` —\n   on a board without edges it simply returns satisfied, so always check.\n   An unmet dependency means pick something else or surface the blockage.\n3. `move_card` to the column whose `column_type` is in-progress/active —\n   it takes a `column_id` and a `position` float (both required; midpoint\n   between the new neighbors, or any float when appending). If the\n   board\'s columns are untyped, consult the definition\'s flow conventions\n   or ask the user — never guess by name.\n4. `add_card_participant` in the same breath as the move (role `helper`,\n   or `hero` for the single responsible owner — a card has at most one\n   hero), plus `update_card` for status/labels. The move and the\n   participant together *are* the interactive claim — a move alone tells\n   nobody who took the card. Resolve which platform member "you" are with\n   `whoami`, and other people\'s `user_id` with `list_workspace_members` —\n   never ask the user for identities the platform can report.\n\n**Never call the runner endpoints from an interactive session.** The two\nclaiming models must not mix:\n\n- *Autonomous runner/pipeline agents*: `next_assignment` is THE pickup\n  path — the backend-owned scheduler atomically reserves the next\n  eligible card and returns it with bundled context. Autonomous flows\n  never search-and-claim manually.\n- *Interactive sessions (humans and interactive agents)*: claiming is the\n  move-plus-participant flow above — column resolved by `column_type`,\n  never by name — and never `next_assignment`. Using the\n  scheduler interactively creates reservations and state the runner world\n  expects to own.\n\nThe same boundary applies to execution logging and the merge queue. One\ninteractive exception in that world: a pending approval gate is *designed*\nfor a human — discover gates with `list_approvals` (`status="pending"`),\ninspect one with `get_approval_status`, answer with `decide_approval`, and\nmake the outcome a real action (proceed/close/block), never just "noted".\n\nMoving through the rest of the lifecycle is the board\'s own business: a\ngovernance board may flow proposal → vote, a delivery board backlog →\nreview → done. Read the columns; respect what the definition says about\nthem; verify every move landed (section 8).\n\n## 5. Notes: durable context, not chat\n\nNotes are how context survives the session. A decision, a handoff, a\nreview summary, or a "state of the world" brief that lives only in\nconversation memory is lost; the same content as a note is recallable by\nevery future session\'s `get_project_context`.\n\n- **Kind:** interactive sessions write `user_note` (the default). The\n  kinds `plan`, `review_verdict`, and `rework_brief` are pipeline-authored\n  artifacts; `system` is a reserved platform kind. Don\'t impersonate\n  them — `review_verdict` notes are immutable once written.\n- **Scope deliberately:** workspace-level for cross-board context,\n  `board_id` for board context, plus `card_id` to attach to a specific\n  card. Card-linked notes are the right home for per-card handoffs and\n  decisions; pinned notes (`pinned: true`) are for the few documents\n  every session should read — pin sparingly.\n- **When to write one:** ending a work session on a card (what was done,\n  what\'s left, where follow-ups live), recording a decision with its\n  rationale, or handing work to someone else. Keep the card description\n  as the spec; notes carry the running history.\n- **Reading notes:** browse with `list_notes` + `summary_only=true`\n  (title/kind/pinned/timestamps only — bodies are ProseMirror JSON, and a\n  handful of full notes can overflow a tool result), then fetch the one\n  you need with `get_note(note_id, format="markdown")` — markdown is the\n  cheap, readable form; take the raw ProseMirror default only when you\n  need the structured tree. Filter listings by `board_id`/`card_id`, and\n  pass the note\'s `board_id` to `get_note` for board-scoped notes.\n\n## 6. Channels: a contact registry, not a chat room\n\nChannels store *how to reach people and systems* — entries typed `email`,\n`slack`, `whatsapp`, `phone`, `website`, or `other`, with the contact\nvalue, at workspace level. There is no message-posting through the MCP;\nconversation happens wherever the team talks.\n\n- Use them to register stakeholder/system contact points once, so every\n  session can find them (`list_channels`); the definition\'s\n  `stakeholders` entries can reference a `channel_id`.\n- Anything that must be *read later by an agent* goes in a note, not in a\n  conversation thread.\n\n## 7. Dependencies: the edges are the truth\n\nProse like "depends on the auth card" is commentary; the machine truth is\nthe edge created with `add_card_dependency` (or\n`bulk_set_card_dependencies` when authoring many). Direction matters:\n`card_id` is the dependent, `depends_on_card_id` is the prerequisite.\n\n- **Anyone who files follow-up work adds the edge.** If work you file\n  blocks an existing card\'s completion, create the edge (the existing\n  card depends on the new one) in the same breath as the card. An\n  edge-less follow-up is invisible to schedulers, dependency status\n  checks, and the next person reading the board. File the follow-up\n  itself into the board\'s backlog/intake-type column (read from the\n  board, same rule as section 4), with `card_type` and `priority` set —\n  `create_card` needs a `column_id` and `title`.\n- After adding edges, sanity-check with `validate_board_dependencies`\n  (cycles, conflicts, orphans); answer "can I start this card?" with\n  `get_card_dependency_status`.\n- Mirror important edges as a human-readable "Depends on" line in the\n  card description if it helps readers — but treat the text as\n  documentation of the edge, never a substitute for it.\n\n## 8. Write discipline: 2xx ≠ persisted, verify by re-read\n\nTwo platform invariants shape every write:\n\n1. **2xx ≠ persisted.** A success status means the request was accepted,\n   not that the field reads back changed. After any write that matters —\n   a definition update, a card move, a label change, anything you are\n   about to report as done — re-read the entity and confirm the new\n   value before declaring success. If the re-read shows the old value:\n   re-issue the identical write and re-read again; after two or three\n   failed cycles, stop and report the discrepancy with both reads —\n   never report success the read didn\'t show.\n2. **Keyed mutations are idempotent.** Adds and creates with a natural\n   identity return the existing entity (200/201) instead of conflicting:\n   participants, dependency edges, labels, definition upserts, board\n   creation *with an explicit `slug`* — and removes of missing things are\n   no-op successes. Retrying these is safe, and a 200 on retry means\n   "already there", not "you just made a duplicate". The exceptions are\n   creates with **no** natural key: a retried `create_card` or\n   `create_note` whose first attempt actually landed makes a real second\n   card/note, and a slugless `create_board` auto-slugs from the name and\n   mints a `-2`-suffixed sibling on collision — pass `slug` to make a\n   board create retryable (and know that re-running `create_board` with a\n   slug *plus* definition fields re-PUTs the definition over the existing\n   one). Before re-issuing an unverified create, re-read first\n   (`search_cards` by title, `list_notes`).\n\nThe deliberate conflicts (409) are semantic, not accidental: adding a\nsecond `hero` to a card (singular by design) and a stale `expected_version`\non `update_workspace_config` (runner-world configs only).\n\nPractical corollaries:\n\n- **Burst mutations hit rate limits.** Past roughly a dozen parallel\n  calls, bulk mutations start failing unpredictably. Batch in modest\n  groups, verify which succeeded (re-read, not assumption), retry the\n  stragglers sequentially. For large card sets prefer one\n  `bulk_create_cards` call over a fan-out of `create_card`.\n- **The activity log is your audit trail.** Mutations are logged;\n  `list_activity` (filterable by entity/action) answers "what changed\n  here, by whom" — check it before re-doing work or when a board looks\n  different than you left it. For runner executions specifically — did\n  an agent touch this card, is a run still in flight — `list_executions`\n  is the read path (`card_id` scopes to that card\'s pipeline history —\n  newest 200 rows, ignoring `limit`; the other filters apply within that\n  window; `status="inflight"` for actively-working rows); *operating*\n  runs belongs to the runner-operations world, not this skill.\n\n## 9. Tool selection: composite first, search over list\n\n| you want | call | not |\n|---|---|---|\n| full project briefing | `get_project_context` | five `list_*` calls |\n| workspace overview / counts | `get_workspace_summary` | listing every board |\n| board shape only (columns, counts) | `get_board` with `summary_only` | full board dump |\n| board risk signals (stale, overdue, unassigned) | `get_board_health` | scanning cards manually |\n| cards matching criteria | `search_cards` (text + priority/type/status/label/assignee/column filters) | `list_cards` + client-side filtering |\n| every card, grouped by column | `list_cards` | repeated searches |\n| many new cards | `bulk_create_cards` | a `create_card` loop |\n| "can this card start?" | `get_card_dependency_status` | walking edges by hand |\n| one note\'s body | `get_note` (`format="markdown"`) | a full `list_notes` dump |\n| a card cited by a short id fragment | `get_card` (accepts a ≥4-char id prefix) | hand-matching against `list_cards` |\n| what this session can call / server version | `get_server_info` | quoting tool counts or surfaces from memory |\n| who this session is | `whoami` | asking the user |\n| pending human gates | `list_approvals` (`status="pending"`) | waiting for an approval id to be handed over |\n\nOne default that bites: untyped columns are human zones (parking lots,\nscratchpads) excluded from pipeline pickup by the scheduler\n(`next_assignment`) — but `search_cards` INCLUDES them by default\n(`include_untyped` defaults to true; omitted means included). A\nuser-facing answer ("what\'s on my board?") can simply omit the flag; any\nwork-pickup-style search MUST pass `include_untyped=false`, or\nhuman-parked scratchpad cards leak into work selection.\n\nRule of thumb: reach for the composite that answers the question in one\ncall; fall back to granular tools to *act*, not to *orient*.\n\n## 10. Which hand for which job\n\nToolsets ENFORCE, skills GUIDE. The server decides which tools a session\ncan list and call — its *hand* — and a skill only says how to use them\nwell. No skill can narrow a listing on any client; what a skill CAN do is\ndeclare the hand it plays in (`toolsets:` in its frontmatter) so the\nplatform warns when guidance and grant disagree. Group ids cover every\ncategory inside them; a category id enables just that slice.\n\n| job | hand | toolset ids |\n|---|---|---|\n| everyday coordination on an existing board (this rulebook, standups, triage) | the default interactive hand | `start-here`, `work-management`, `knowledge-content` — minus the destructive/admin tools (delete_workspace, delete_board, delete_column, freeze_board, unfreeze_board, member management) and the runner-only pickup path (next_assignment), plus a few read-only helpers from other groups (list_skills, get_skill, list_git_repos, get_workspace_metrics) |\n| bootstrapping a workspace or board: repos, channels, teams, webhooks | default + collaboration | add `collaboration` (or only `git-repos`, `channels`, `teams`, `webhooks`) |\n| configuring, launching, or watching autonomous runs, approvals, merge queue, loops | default + autonomous operations | add `autonomous-operations` (or only `agents-executions`, `approvals`, `merge-queue`, `workspace-config`, `prompt-configs`, `skills`) |\n| a runner pipeline stage | the stage\'s own grant | the runner launches the server with `VALARIS_MCP_ALLOWLIST`, so the stage\'s tool list IS its hand and every toolset is in scope; nothing to enable |\n| a loop-mode board | the loop\'s `tools` grant | `loop_config.tools` (prefixed `mcp__valaris__`; empty = the whole surface). Bound skills whose declared toolsets the grant cannot fully cover are reported as `uncovered_toolsets` on the board\'s skill bindings — widen `tools`, not the skill |\n\nEnable a hand for an interactive session with one environment variable on\nthe MCP server (`default` is an alias for the interactive hand, `all`\nloads every tool, an unknown id fails startup):\n\n    VALARIS_MCP_TOOLSETS=default,collaboration\n\nOmit it for the default hand. `get_server_info` reports the hand actually\nin force, what else exists, and how to widen it — trust it over memory.\n',
        ),
        CatalogFile(
            path='references/tool-map.md',
            content='# Backplane MCP tool map — interactive surface\n\nTool-by-tool reference for human/interactive-agent sessions. Tools are\nnamed by their bare logical names; your client may expose them under a\nprefix. Runner-world tools are listed only to mark the boundary.\n\n## Session start: census and identity\n\n| tool | notes |\n|---|---|\n| `get_server_info` | server version + every registered tool + `enabled_tools` for THIS session (registered ∩ allowlist) + backend reachability — the mechanical answer to "does tool X exist here?". A "missing" tool is usually version or allowlist drift; this call is the census — never hardcode or quote tool counts from memory |\n| `whoami` | the authenticated platform user behind this session (id, email, name) — how you learn your own `user_id` before adding yourself as a card participant, and which account the session acts as. Distinct from `get_agent_config` (the runner AGENT\'s identity). Replaces asking the user "which member am I?" |\n\n## Composite reads (orient with these)\n\n| tool | returns |\n|---|---|\n| `get_project_context` | one-call briefing: board summary + definition + notes + git repos + recent activity |\n| `get_workspace_summary` | aggregate stats: board/card/note/channel counts + recent activity |\n| `get_board` | full board: columns (each with its `column_type` — how you identify the in-progress/backlog/review/done columns) + cards (position, status, priority, assignee, labels); `summary_only` flag for shape + distributions only |\n| `get_board_health` | health score, stale/overdue/unassigned counts, velocity |\n| `list_activity` | audit trail, newest first; filter by entity_type/action/search; workspace- or board-scoped |\n\n## Workspaces & boards\n\n| tool | notes |\n|---|---|\n| `list_workspaces` / `get_workspace` | discovery / single workspace (`get_workspace` does NOT return members — see `list_workspace_members`) |\n| `create_workspace` | new workspace, creator becomes owner; `slug` derived from `name` when omitted; calling it with an existing slug is a successful no-op that returns the existing workspace (under `existing`), never an error. Bootstrapping a whole project is the Project Bootstrap skill\'s job |\n| `list_boards` | boards in a workspace |\n| `create_board` / `update_board` | idempotent ONLY with an explicit `slug` (existing board returned, not conflicted); a slugless call auto-slugs from the name and mints a NEW `-2`-suffixed sibling on collision — always pass `slug` for retryable creates. Accepts full definition fields inline; re-running with `slug` + definition fields re-PUTs (overwrites) the definition |\n| `delete_board` | IRREVERSIBLE cascade — removes the board\'s columns, cards (with participants and dependencies), notes, definition, git-repo bindings, and resources; confirm the board id (`list_boards`) and the intent first |\n| `create_column` / `update_column` / `delete_column` / `reorder_columns` | column management; `column_type` matters to the runner world — don\'t set or change types casually on a runner-bound board |\n\n## Definition (per board)\n\n| tool | notes |\n|---|---|\n| `get_definition` | `scope` (text) + `content`: `objectives` (list of `{text, priority}`), `exclusions` (strings), `milestones` (`{title, date, type}`), `tech_stack` (strings), `stakeholders` (`{name, role, member_id, channel_id}`), `constraints` (strings), `decisions` (`{decision, rationale}`), `references` (`{url, label}`), `custom_fields` (`{key, value}`), `coding_standards` (text) |\n| `update_definition` | shallow-merge upsert: untouched fields are preserved, but a list field you send is replaced whole — read-modify-write complete lists; no version guard — re-read to verify |\n\n## Cards\n\n| tool | notes |\n|---|---|\n| `list_cards` | whole board grouped by column (includes untyped columns) |\n| `search_cards` | text query + filters: priority, card_type, status, label, has_assignee, assignee_id, column_id, column_type, exclude_column_type, overdue, limit, `include_untyped` (default true — omitted means INCLUDED; autonomous/pickup-style scans must pass `include_untyped=false` explicitly, or human-parked scratchpad cards leak into work selection; user-facing board reads can simply omit it) |\n| `get_card` | single card detail; `card_id` takes the full UUID or a ≥4-char id prefix (the fragment style notes and standups use) — a unique prefix resolves to the card, an ambiguous one errors listing candidates |\n| `create_card` | requires `column_id` + `title`; card_type: task/issue/feature/bug; priority: none/low/medium/high/urgent; NOT idempotent — an unverified retry can duplicate (re-read first) |\n| `bulk_create_cards` | prefer over a `create_card` loop for many cards (rate limits) |\n| `update_card` | fields: title, description, card_type, priority, due_date, status, labels, git_repo_slug — labels are replaced as a set, read-modify-write |\n| `move_card` | requires `column_id` AND `position` (float — midpoint between the new neighbors, or any float when appending); column names are never arguments |\n| `delete_card` | destructive — confirm before using |\n| `add_card_participant` | role: `hero` (single responsible owner — second hero conflicts), `helper`, `viewer`, `stakeholder`; idempotent re-add |\n| `remove_card_participant` | `user_id` (one person) or `pipeline_role` (every holder of a stage role); removes are no-op-safe |\n\n## Dependencies\n\n| tool | notes |\n|---|---|\n| `add_card_dependency` | `card_id` (the dependent) + `depends_on_card_id` (the prerequisite); idempotent re-add; cycles rejected |\n| `remove_card_dependency` | no-op-safe remove |\n| `bulk_set_card_dependencies` | replace a card\'s edge set in one call |\n| `list_card_dependencies` | edges for a card |\n| `get_card_dependency_status` | "can this start?" — satisfied flag + blocking list; safe on edge-less boards (returns satisfied) |\n| `validate_board_dependencies` | whole-board check: cycles, conflicts, orphans |\n\n## Notes\n\n| tool | notes |\n|---|---|\n| `create_note` | kind defaults to `user_note` (the interactive kind); `pinned` flag; scope via optional `board_id`, attach via optional `card_id`; NOT idempotent — an unverified retry can duplicate (re-read first) |\n| `list_notes` | browse with `summary_only=true` (drops bodies — they are ProseMirror JSON, and a few full notes can overflow a tool result); filter by `board_id`/`card_id`; then `get_note` for the one you need |\n| `get_note` | single-note fetch by `note_id`; `format="markdown"` returns the body as readable markdown — use it unless you need the raw ProseMirror tree; pass the note\'s `board_id` for board-scoped notes |\n| `update_note` / `delete_note` | `review_verdict` notes are immutable; pipeline kinds (`plan`, `rework_brief`, `system`) are not yours to edit |\n\n## Channels, resources, members, git repos\n\n| tool | notes |\n|---|---|\n| `create_channel` / `list_channels` / `update_channel` / `delete_channel` | workspace-level contact registry; types: email, slack, whatsapp, phone, website, other; no message posting via MCP |\n| `create_resource` / `list_resources` / `get_resource` / `update_resource` / `delete_resource` | shared files/links; `get_upload_url` / `get_download_url` for content |\n| `list_workspace_members` | the roster (`user_id`, email, name, role) — the resolver for participant `user_id`s (`get_workspace` omits members); optional `query` + `limit` for autocomplete-style matching |\n| `add_workspace_member` / `remove_workspace_member` | workspace membership |\n| `list_git_repos` | which repo(s) a board is bound to — read freely |\n| `create_git_repo` / `update_git_repo` / `delete_git_repo` | repo binding is load-bearing for runner-bound boards; keep one repo per board and one board per repo as house policy (the platform itself allows multi-repo boards via per-card `git_repo_slug`, but two boards driving one repo cross-block each other\'s implementers) — change only as part of a deliberate runner-preparation pass |\n\n## Approvals (the human side)\n\n| tool | notes |\n|---|---|\n| `list_approvals` | discover approvals when you hold no approval id — filter by `status` (`pending` is what approvers want), then act with `decide_approval` |\n| `get_approval_status` | check one approval gate by id |\n| `decide_approval` | the human decision (approve/reject) — every outcome must map to a real action (proceed/close/block) |\n| `request_approval` | normally pipeline-originated; interactive sessions rarely need it |\n\n## Runner world — off-limits interactively\n\n`next_assignment`, `log_execution_start` /\n`log_execution_update`, `cancel_execution`, agent tools (`create_agent` /\n`update_agent` / `get_agent` / `get_agent_config`), team tools\n(`create_team`, `add_team_member`, …), prompt configs\n(`*_prompt_config`), pipeline bundles (`export_pipeline_bundle` /\n`import_pipeline_bundle`), `get_workspace_config` /\n`update_workspace_config`, merge queue (`enqueue_for_merge`, …),\n`get_pipeline_sensors`, webhooks (`create_webhook` / `list_webhooks` —\nintegration plumbing), and the loop-profile authoring/fit tooling.\n\nThese belong to autonomous runner operation. The two claiming models\nnever mix: for autonomous runner/pipeline agents, `next_assignment` is\nTHE pickup path — the backend-owned scheduler atomically reserves the\nnext eligible card and returns it with bundled context; autonomous flows\nnever search-and-claim manually. In interactive sessions, "claim a card" =\n`move_card` to the column resolved by `column_type` (never by name) plus\n`add_card_participant` (see SKILL.md section 4).\n\nOne read is fine interactively: `list_executions` — workspace execution\nhistory, newest first; filters `status` / `agent_id` / `role` /\n`card_id`, `limit` default 20, backend cap 200; `card_id` is a\nserver-side scope returning that card\'s newest 200 rows, ignoring\n`limit` (`status`/`agent_id`/`role` filter within that window);\n`status="inflight"` is a virtual filter for every actively-working row,\nthe zombie-finder that pairs with `cancel_execution`. Reading it to\nanswer "did an agent touch this card?" is fair game; *operating* a run\nwith it belongs to runner-run monitoring, outside this skill.\n',
        ),
    ),
)
