# Copyright (c) 2026 Valaris Studio
# SPDX-License-Identifier: AGPL-3.0-or-later

"""Catalog entry: Autonomous Run Monitoring (generated from the verified source bundle;
do not hand-edit the embedded content)."""

from app.services.skills.catalog_types import CatalogEntry, CatalogFile

ENTRY = CatalogEntry(
    catalog_id='autonomous-run-monitoring',
    catalog_version=2,
    name='Autonomous Run Monitoring',
    description='Operates a live autonomous runner run on a Backplane board — run signals, wedge and money-loop diagnosis, kill-vs-wait, cleanup recipes; use when a run looks stuck, idle, looping, or expensive, or when reconciling after a pause or kill.',
    files=(
        CatalogFile(
            path='SKILL.md',
            content='---\nname: Autonomous Run Monitoring\ndescription: Operates a live autonomous runner run on a Backplane board — run signals, wedge and money-loop diagnosis, kill-vs-wait, cleanup recipes; use when a run looks stuck, idle, looping, or expensive, or when reconciling after a pause or kill.\ntoolsets: [start-here, work-management, knowledge-content, autonomous-operations]\n---\n\n# Autonomous Run Monitoring\n\nOperate a live autonomous run on a Backplane board: read the signals,\nclassify what you see, intervene with the smallest reversible lever, and\nnever lose work — **board state IS the durable state**; no work is lost\nacross pauses.\n\nCompanion to the Agentic Board Setup skill (which configures the board and\nhands you a launch watch-list). This skill assumes a run is launched or just\nended.\n\n## The diagnostic lens\n\nAlmost every structural incident is one shape: **a stage reaches a clean exit\nWITHOUT consuming a card or producing a real artifact, and the system treats\nthat as work (re-loop), failure (re-reserve), or nothing (dead-end).** When\nsomething looks wrong, ask: *which produced signal (label, note, decision,\nfix card) has no consumer — or which consumer is seeing a no-op as\nwork/failure?* The full catalog of known instances, with fixes, is in\n`references/failure-taxonomy.md` — match symptoms against it before inventing\na new theory.\n\n## Covers / does not cover\n\n**Covers:** reading live signals (logs, board activity, executions, costs),\nclassifying known wedge/loop patterns, kill-vs-wait decisions, reversible\nboard-lever interventions, post-pause cleanup recipes, and verifying a fix\ntheory before applying it.\n\n**Does not cover:** board/pipeline setup (companion skill), patching backend\ncode or rebuilding the runner binary (you may *identify* that a fix needs\nthose levers and escalate), product-level QA of what the run shipped (a\ndone-vs-working audit, a separate activity).\n\n## Watch the right streams\n\nSession start: `get_server_info` — the mechanical census of the MCP surface\n(server version, registered tools, allowlist, backend reachability). A recipe\ntool that "doesn\'t exist" is usually version drift or an allowlist\nrestriction, not a broken server; verify before improvising around it.\n\n- **The runner log** (the launch should have tee\'d one): stage starts/ends,\n  exit codes, token/cost lines, reservation activity.\n- **Board activity** (`list_activity`) and card state (`list_cards`,\n  `get_card`): column moves, participant add/remove pairs, label changes.\n- **Executions** (`list_executions`): the run\'s audit trail, newest first.\n  `status="inflight"` is a virtual filter — every actively-working row\n  (started/running), unbounded by limit — the direct zombie hunt.\n  `card_id=` is a server-side card scope — the card\'s pipeline history,\n  ignoring `limit` but capped at the newest 200 rows (a runaway loop card\n  can truncate); `status`/`role`/`agent_id` filter within that scope. Rows\n  are heavy (full input_prompt + tool invocations): keep `limit` small\n  (default 20, backend caps at 200).\n- **Health/sensors**: `get_board_health`, `get_pipeline_sensors`.\n- **Merge queue** (`list_merge_queue`): entries in\n  `queued | merging | conflict | failed` — successfully merged entries drop\n  out, so anything lingering is triage material. Only `queued`/`merging`\n  entries hide their card from `next_assignment`; a `conflict`/`failed`\n  entry does NOT block scheduling — it is lingering triage evidence.\n- **Cost**: the authoritative stream is the runner log\'s per-stage\n  token/cost lines — sum them per 15-minute window against the\n  circuit-breaker threshold; `get_pipeline_sensors` exposes the same where\n  supported, and without log access `list_executions` rows carry per-stage\n  `cost_usd`/`tokens_used` (`card_id=` totals a suspect card\'s spend —\n  newest 200 rows, so a long loop under-counts).\n- **Approvals**: a card blocked on an undecided `request_approval` looks\n  like an unexplained stall — sweep `list_approvals(status="pending")` when\n  you hold no approval id, then `get_approval_status` / `decide_approval`\n  (every approval outcome must map to a terminal action).\n\n## Signal table (memorize the first three)\n\n| signal | meaning | action |\n|---|---|---|\n| `exit_code=1` with `cost_usd=0, tokens_in=0, tokens_out=0` (often `output_len=66`) | **provider quota/credits exhausted** — an environment failure, NOT a card defect | Binary with the empty-LLM breaker: it pauses ~15 min after 3 consecutive empties and probes; wait. **Vintage tell:** 3+ consecutive $0 failures with NO pause means the breaker is absent — treat as old binary and KILL NOW (it spins: re-reserve + participant churn + backend 429s). |\n| Card bouncing column↔column ~every poll with participant add/remove pairs | a **money-loop** (re-reserve class) | Kill; find which produced signal lacks a consumer (taxonomy). `list_executions(card_id=…)` is the loop counter — each cycle with its cost, capped at the newest 200 rows (a prod loop hit 249 cycles, so the count can under-read). |\n| Backend HTTP 429 storms | call-rate burst, usually downstream of an empty-LLM spin or unthrottled refresh loop | Find the upstream spin; kill if old binary. |\n| Same Done card re-reserved minutes after a clean approve | reservation-eligibility split-brain or missing dependency gate | Kill + patch config/backend. |\n| Planner idle while backlog has eligible cards | a stranded card tripping the planner\'s in-flight gate, OR the **repo-wide open-PR gate**, OR a label wedge | Board lever first — hunt in order: (1) backlog card with `planned` + 0 participants, (2) any non-backlog card no role can act on (excluded label, no participants), (3) an orphaned open PR, (4) label wedges. Fixing the board resumes planning on the next poll — no restart. |\n| Card in **backlog** with `planned` label and 0 participants | fail-path partial abort stranded it in a discover dead-zone | Move it to active (reversible); it self-heals from there. |\n| Approved card frozen — merge never lands, no role touches it again | a `conflict`/`failed` merge-queue entry is EVIDENCE of the failed merge, not the freeze mechanism (only `queued`/`merging` entries hide a card from `next_assignment`) — the actual wedge is usually the orphaned open PR tripping the repo-wide `repo_has_no_open_pr` gate, else the done-gate or an approval park | `list_merge_queue` / `get_merge_queue_entry` for the evidence, then hunt the real gate (orphaned-PR recipe, approvals sweep); the merge-queue cleanup recipe below is hygiene, not the un-freeze. |\n| Card in **active** with 0 participants | benign — the implementer\'s unassigned-or-rework discover self-heals it | Wait. |\n| WebSocket disconnect/reconnect every ~5 min during long stages | infra idle-recycle, ~1 s reconnect — benign | Ignore unless the cadence collapses to rapid succession. |\n| `agent_presence: touched` on a card | computed read-time annotation; does not gate scheduling | Nothing to clear. |\n| ~18 min idle-but-alive with in-flight executions | often just a long LLM stage or a poll gap | Check executions before assuming a wedge. |\n\n## Kill vs wait\n\n**Definitions.** *Kill* = stop the runner process itself — Ctrl-C/SIGTERM in\nthe launch terminal (the setup skill launches it as a tee\'d foreground\ncommand). Reservations expire on their own; afterwards run the cleanup\nrecipes. *Pause* = the breaker-initiated state (cost or empty-LLM breaker):\nautomatic, heartbeat kept, self-resumes at expiry — not an operator action.\n\n- **Kill** on structural loops: money-loops, quota-spin on an old binary,\n  429 storms, post-approve re-reservation. **Leave it killed until the\n  structural fix lands** — re-launching reproduces the wedge.\n- **Prefer reversible board levers over restarts** for stranded-state\n  wedges: move a card, strip a label, add a dependency edge, reap a zombie\n  execution. The runner self-resumes on its next poll (~2 min) without ever\n  stopping — a board edit has un-wedged a run in under 30 seconds in\n  production.\n- **Wait** on anything in the benign rows, and on the new binary\'s breaker\n  pauses (expiry is the probe).\n\n## Cleanup recipes (post-pause / post-kill reconciliation)\n\n- Backlog card with `planned` + 0 participants → `move_card` to active.\n- Active card with 0 participants → leave it; the implementer self-heals it.\n- Zombie `running` executions (especially with null cards_affected) →\n  `list_executions(status="inflight")` lists every actively-working row,\n  each carrying the `agent_id` + execution id that `cancel_execution`\n  needs; reap the wedged ones to `aborted` — this clears the agent-busy\n  guard so the runner can reserve work again on its next poll.\n- Wedged merge-queue entry (`conflict`/`failed`, or `queued` long after the\n  worker should have run) → inspect with `list_merge_queue` +\n  `get_merge_queue_entry`; `cancel_merge_queue_entry` (admin/owner) deletes\n  the row — hygiene, and it takes the merge out of the automated path. It\n  only un-hides the card when the entry was `queued`/`merging` (the sole\n  states `next_assignment` excludes); a `conflict`/`failed` entry was never\n  blocking scheduling. After resolving a conflict, prefer re-queueing the\n  EXISTING entry with `enqueue_for_merge` over cancelling: it is\n  RE-enqueue-after-conflict only — it re-queues a card\'s EXISTING entry and\n  404s when none exists; never a generic "add to the merge queue".\n- Run stalled on an approval gate → `list_approvals(status="pending")` to\n  discover the undecided requests, then `decide_approval` each with an\n  explicit reason — an approval left pending across a relaunch stalls the\n  same card again.\n- Orphaned open PR (merge failed, card bounced) → if the card was superseded\n  or is a duplicate, close the PR; otherwise resolve the conflicts and\n  `move_card` the card to the review column — the reviewer rediscovers it\n  via its open-PR discover gates. Either way act now: **it is blocking ALL\n  implementer pickup on the board** until handled.\n- Stray routing label to strip → `update_card` with the corrected label set.\n- Auditor loop in progress → manually add the missing dependency edges with\n  `add_card_dependency` (source depends_on each fix card) and ensure the fix\n  cards sit in active with `direct-implement`.\n\n## Verify before you fix\n\nAdversarially verify a wedge theory with instrumentation before acting — a\nconfident theory has been flatly wrong in production before; cheap evidence\n(a queue length, a re-read of the actual discover filter, one poll cycle of\nlogs, a card\'s execution history via `list_executions(card_id=…)`) settles\nit. Standing traps:\n\n- **2xx ≠ persisted.** After any config/board write, re-read and confirm the\n  field actually changed before declaring it fixed.\n- **Stored-prompt reality.** The runner executes stored prompt rows, not\n  registry defaults; deleting a "stale" override falls back to the built-in\n  prompt, which may silently drop the fix you\'re counting on.\n- **Stored-config reality.** A live board usually runs a stored config\n  override, so a fix landed as a backend/source-code default never reaches\n  it on its own — it also needs a parallel live config patch\n  (`update_workspace_config` or a bundle import), verified by re-read,\n  before you may declare the structural fix landed and relaunch.\n\nWhen the fix requires a config patch, remember discover predicates can live\nin **two places** (the flat discover block and the lifecycle discover step) —\npatch both, then re-read both.\n\n## Escalation boundary\n\nYou own: board levers, config patches (with re-read verification),\nkill/pause decisions, cleanup. Escalate with evidence (signal, theory,\nverification): backend deploys, runner binary rebuilds, anything touching the\nprovider account, and any product-level finding (the run shipped something\nbroken) which belongs in a done-vs-working audit, not a live intervention.\n',
        ),
        CatalogFile(
            path='references/failure-taxonomy.md',
            content='# Failure-class taxonomy — every known instance of the North-Star class\n\n**The class:** *a stage reaches a clean exit WITHOUT consuming a card or\nproducing a real artifact, and the system treats that as work (re-loop), as\nfailure (re-reserve), or as nothing (dead-end) — instead of as IDLE (release\ncleanly).* Money-loops, phantom cards, poll storms, label wedges, and\ndiscover dead-zones are all instances. When diagnosing, ask of every signal\nthe run produces: *who consumes this, and what happens if nobody does?*\n\nMatch an observed symptom against the catalog below before inventing a new\ntheory. FIX-CLASS tells you the smallest lever that cured it (board lever <\nconfig patch < backend < binary). STATUS tells you whether the cure is baked\ninto the platform/default config or whether you must verify it is present\nbefore relying on it.\n\n## Catalog of known instances\n\n### 1. Orphaned-PR wedge\n\n- **SYMPTOM:** Implementer never picks up any card on the board; an open PR\n  from a failed merge is sitting on the repo.\n- **MECHANISM:** A failed merge produced a conflict outcome with no consumer\n  (the merge step had no `on_failure` path). The PR stayed open, and the\n  implementer\'s `repo_has_no_open_pr` precondition is **repo-wide** — one\n  orphaned open PR blocks every implementer pickup on the board.\n- **FIX-CLASS:** Config patch (merge-conflict `on_failure` edge routing into\n  the rework machinery) + backend (zombie-execution reaper that doesn\'t gate\n  on a nullable field). Live cure: board lever — rebase/resolve and\n  re-queue, or close the PR. When the wedge is merge-queue-side,\n  `list_merge_queue` / `get_merge_queue_entry` show the stuck entry;\n  `cancel_merge_queue_entry` deletes the row for hygiene, but only un-hides\n  the card when the entry was `queued`/`merging` — the sole states\n  `next_assignment` excludes. A `conflict`/`failed` entry never blocked\n  scheduling: here the open PR is the actual gate. Note `enqueue_for_merge`\n  only RE-queues a card\'s existing entry after a conflict consolidation\n  (404 without one) — it cannot create entries.\n- **STATUS:** The self-heal edge is in the default pipeline; **must-verify**\n  on any older or hand-edited config before relying on it.\n\n### 2. Pre-seeded routing-label wedge (chicken-and-egg)\n\n- **SYMPTOM:** Cards sit in backlog forever carrying a late-stage routing\n  label (e.g. `needs-ui-validation`); no role ever touches them.\n- **MECHANISM:** Every build role `exclude_label`s the validation label, and\n  the validator only scans the done column — pre-seeding the label at an\n  early stage means the card can never be built, so it can never reach the\n  stage that needs the label. Rule: a routing label that gates a LATE stage\n  must never be applied at an EARLY stage. The sanctioned mechanism is a\n  machine-readable body marker (`Validation: requires-ui-validation`) that\n  the reviewer converts to the label on the APPROVE path.\n- **FIX-CLASS:** Board lever (strip the label; the runner picked up the\n  unwedged card within seconds of the edit, no restart) + authoring\n  discipline going forward.\n- **STATUS:** Prevention is a card-authoring rule, not a platform fix —\n  **must-verify** every board\'s labels at launch (zero pre-seeded routing\n  labels).\n\n### 3. Re-validation money-loop (auditor with no dependency edge)\n\n- **SYMPTOM:** The validator re-audits the same source card every poll,\n  burning real cost per cycle, while the fix cards it spawned never get\n  worked. `list_executions(card_id=…)` on the source card shows the cycle\n  count and per-cycle cost.\n- **MECHANISM:** Validator FAIL spawned fix cards with no dependency edge\n  back to the source. With nothing blocking it, the source stayed eligible\n  and was re-audited every poll while the single agent starved the fixes.\n- **FIX-CLASS:** Backend (`create_fix_cards` now auto-adds *source\n  depends_on fix_card*) + config (`all_dependencies_done` on the\n  validator\'s discover; raw `create_card` removed from its tool allowlist —\n  an engine kind that owns labels+edges must not coexist with the raw tool).\n- **STATUS:** Fixed-in-platform for engine-spawned fix cards. **Must-do\n  manually** for hand-filed fix cards: add the edges yourself or the loop\n  returns. Live cure: board lever (add the missing edges; put fixes in\n  active with `direct-implement`).\n\n### 4. No-diff "duplicate" money-loop\n\n- **SYMPTOM:** The implementer repeatedly picks up the same card, produces\n  no diff, and the card re-reserves every cycle at steady cost.\n- **MECHANISM:** A clean-tree implement (work already shipped) had only one\n  meaning to the runner: failure → re-reserve. Approvals existed but were\n  decorative — no outcome mapped to a terminal action, so "approved" merely\n  let the next identical pass run.\n- **FIX-CLASS:** Config/prompt (the LLM declares `resolution: "duplicate"`\n  → approval-gated terminal close: Done + `duplicate` label) + binary (a\n  consecutive-no-change-implements backstop force-blocks any undeclared\n  repeated no-op, regardless of prompt quality).\n- **STATUS:** **Must-verify** the launch binary carries the no-diff\n  backstop; the declared-duplicate path must exist in the stored prompt row.\n  Lessons encoded: name the reason, not the mechanism; gate self-declared\n  verdicts (the gate caught a confidently-wrong duplicate verdict live);\n  every approval outcome must map to a terminal action.\n\n### 5. Stale-PR-branch phantom cards\n\n- **SYMPTOM:** Post-merge validation keeps "finding" bugs that were already\n  fixed and merged; phantom fix cards appear and feed a no-diff loop.\n- **MECHANISM:** The post-merge validator checked out the frozen PR branch\n  instead of the integrated result, so it audited a stale snapshot and\n  re-discovered already-fixed defects. Invariant: pre-merge stages audit the\n  candidate (PR branch); post-merge stages audit the integrated result\n  (integration head) — split the checkout action, never overload one.\n- **FIX-CLASS:** Backend (the integration-head checkout action must be known\n  to the deployed validator — configs using it are rejected until then) +\n  binary (the running runner must know how to execute it) + config (the\n  validator stage must use it).\n- **STATUS:** **Must-verify** all three layers: deployed backend accepts the\n  action, the binary executes it, the config uses it (not the PR-branch\n  checkout) on the post-merge validator.\n\n### 6. Empty-decision dead-end\n\n- **SYMPTOM:** A card hard-errors and dead-ends after a decision stage that\n  produced no verdict.\n- **MECHANISM:** A `produces_decision` step returning an EMPTY decision was\n  treated as a hard error with no consumer. Now it soft-fails to the step\'s\n  `on_failure` path — which only helps if that path exists.\n- **FIX-CLASS:** Backend/engine (empty decision → soft-fail) + config (every\n  `llm` / merge / PR-creation step needs an `on_failure`).\n- **STATUS:** Engine side fixed-in-platform; the `on_failure` edges are\n  config and **must-verify** per stage.\n\n### 7. Self-trigger poll storm\n\n- **SYMPTOM:** The runner polls in a tight loop (many polls per second) with\n  no card ever consumed.\n- **MECHANISM:** A lifecycle walk that reached a clean exit without\n  consuming a card still reported "had work" → the self-trigger re-armed\n  every tick. The purest form of the class: a no-op read as work.\n- **FIX-CLASS:** Binary (clean no-card walk = IDLE, plus a self-trigger\n  rate-floor of a few seconds).\n- **STATUS:** **Must-verify** the launch binary carries the fix.\n\n### 8. Disabled-role phantom work\n\n- **SYMPTOM:** The role scheduled after a disabled role silently never runs;\n  the scheduler walk always ends one slot before it.\n- **MECHANISM:** A disabled role\'s minimal lifecycle (a bare end step)\n  walked cleanly with no card and reported work, ending the walk and\n  starving every role after it — no error anywhere.\n- **FIX-CLASS:** Binary/engine (clean walk with no card = IDLE).\n- **STATUS:** Fixed-in-platform; standing rule remains: keep disabled\n  roles\' lifecycles minimal.\n\n### 9. Reservation-eligibility split-brain\n\n- **SYMPTOM:** An already-approved card is re-handed to a role every poll\n  after a mid-reservation label change.\n- **MECHANISM:** The reservation-still-eligible check lacked label\n  predicates that the candidate-selection check had — the two halves of\n  discover disagreed about the same card. Standing trap: discover predicates\n  must live in BOTH the candidate query and the reservation-eligibility\n  check, and the filters also appear twice in the config (flat `discover`\n  block + the lifecycle discover step params) — patches must hit both spots.\n- **FIX-CLASS:** Backend (predicate parity) + config hygiene (patch both\n  spots, re-read both).\n- **STATUS:** Fixed-in-platform for the known predicates; **must-verify**\n  parity whenever the platform adds a new predicate.\n\n### 10. Empty-LLM spin ($0 quota loop)\n\n- **SYMPTOM:** `exit_code=1` with `cost_usd=0, tokens_in=0, tokens_out=0`,\n  instant turnaround, reserve→fail→re-reserve forever, participant\n  add/remove churn, backend 429 storms. The execution trail\n  (`list_executions`) shows instant `failed` rows at `cost_usd=0`.\n- **MECHANISM:** Provider quota/credit exhaustion returns instantly at zero\n  cost — invisible to the cost circuit breaker, which only sees dollars. The\n  runner read each $0 failure as a card defect and re-reserved. Environment\n  failure ≠ card defect.\n- **FIX-CLASS:** Binary (consecutive-empty-result breaker: several empty\n  results in a row → pause reservations/refreshes with heartbeat kept;\n  expiry acts as the probe).\n- **STATUS:** **Must-verify** the launch binary carries the breaker. On an\n  old binary this is a kill-now signal.\n\n### 11. Fail-path partial abort (stranding + board-wide freeze)\n\n- **SYMPTOM:** A card sits in backlog with the `planned` label and zero\n  participants; separately, the planner goes idle even though eligible\n  backlog cards exist.\n- **MECHANISM:** Two stages of one wedge. (a) The implementer fail path runs\n  unassign-then-move; a transient 429/timeout between the steps strands the\n  card in a discover dead-zone (backlog + `planned`: planner excludes the\n  label, implementer never scans backlog). (b) The stranded card then\n  tripped `no_other_card_in_flight` and froze ALL planning — cured by\n  redefining in-flight to count only actively-worked siblings (a participant\n  row or an unexpired reservation); a card no role can act on is NOT in\n  flight.\n- **FIX-CLASS:** Backend (the in-flight redefinition). The stranding itself\n  wants a config-shape fix (column-restore BEFORE unassign; cleanup calls\n  best-effort) — until that is in your config, the cure is a board lever:\n  move the card to active and it self-heals.\n- **STATUS:** **Must-verify** the deployed backend carries the in-flight fix\n  before relying on it; the stranding remains possible and the board-lever\n  recipe remains the cure.\n\n### 12. The product-level instance (the biggest)\n\n- **SYMPTOM:** Every card lands clean — unit-green, PR merged, healthy\n  efficiency metrics — yet the shipped product cannot perform its core\n  function at all.\n- **MECHANISM:** The board-level analogue of the class: every card reached a\n  clean per-card exit **without the product consuming the work**. Fakes and\n  stubs sat at every production seam (a fake capturer in the prod\n  construction path, a silent recognizer returning empty output, detection\n  signals produced but never pumped into the orchestrator), each card\n  deferred wiring to "the next card" and no card was the next card,\n  cross-card bugs were invisible to both cards\' unit tests, and the final\n  artifact was never even built. Unit-level Definition-of-Done ≠ working\n  product.\n- **FIX-CLASS:** Card authoring (board-setup discipline, owned by the\n  companion skill): wire-or-file at every production seam; explicit\n  integration cards with end-to-end DoD; a final acceptance/milestone card\n  depending on everything that builds and click-throughs the real artifact;\n  grep-able "no fake in prod path" assertions; validator activation via the\n  body marker on every UI card.\n- **STATUS:** Prevention only — no platform fix exists or can exist. If a\n  live run shows this, it belongs in a done-vs-working audit, not a live\n  intervention.\n\n## Design heuristics distilled\n\nEvery config and card decision should survive these eight checks:\n\n1. **Every produced signal needs a named consumer** (note, label, decision,\n   fix card — if nothing consumes it, it WILL loop or strand).\n2. **Every approval needs a terminal action** (close/proceed/block — an\n   approval that merely lets the next pass run is decorative).\n3. **Every auditor\'s spawned work blocks the audited item** (dependency edge\n   from source to each fix card, always).\n4. **Every fail path ends somewhere discoverable** (audit it against the\n   role/column-type matrix: which role scans where the card lands?).\n5. **Every no-op is IDLE** (never failure-that-re-reserves, never\n   success-that-loops).\n6. **Every breaker has a $0-blind-spot check** (cost breakers cannot see\n   zero-cost failures).\n7. **Every label gate is checked against "can the card still reach the stage\n   that needs it"** (the chicken-and-egg test).\n8. **Every seam fake has a card with an edge** (a code comment is not a\n   tracker).\n\n## Verify the theory first\n\nAdversarially verify a wedge theory with cheap instrumentation **before**\nfixing: a confident theory has been flatly wrong in production — a single\nqueue-length measurement disproved it. One re-read of the actual discover\nfilter, one poll cycle of logs, one counter is enough to confirm or kill a\ntheory; a fix applied to the wrong mechanism adds a new instance of the\nclass instead of removing one. The cheapest counters are one call away:\n`list_executions(card_id=…)` is a card\'s full cycle history with per-stage\ncost, and `list_executions(status="inflight")` is the live in-flight set.\n',
        ),
    ),
)
