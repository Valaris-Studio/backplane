# Copyright (c) 2026 Valaris Studio
# SPDX-License-Identifier: AGPL-3.0-or-later

"""Catalog entry: Project Bootstrap (generated from the verified source bundle;
do not hand-edit the embedded content)."""

from app.services.skills.catalog_types import CatalogEntry, CatalogFile

ENTRY = CatalogEntry(
    catalog_id='project-bootstrap',
    catalog_version=2,
    name='Project Bootstrap',
    description='Bootstraps a Backplane project from a raw brief — workspace-vs-board choice, workflow-matched columns, rich definition, seed notes/channels, repo binding, verified starter backlog; use when creating or initializing a board or workspace.',
    files=(
        CatalogFile(
            path='SKILL.md',
            content='---\nname: Project Bootstrap\ndescription: Bootstraps a Backplane project from a raw brief — workspace-vs-board choice, workflow-matched columns, rich definition, seed notes/channels, repo binding, verified starter backlog; use when creating or initializing a board or workspace.\ntoolsets: [start-here, work-management, knowledge-content, collaboration]\n---\n\n# Project Bootstrap\n\nTurn a project brief — or a pile of unorganized context — into a working\nBackplane board for **interactive, human-first** coordination: the right\ncontainer, columns that match how the team actually works, a definition\nrich enough that every future agent session finds its project nouns\nthere, seed content, and a starter backlog. Leave clean handoff points\nfor when the project later goes agentic.\n\nThe platform ships a server-side `init_project` prompt (Project\nInitializer role); this skill follows its **phase order** — understand →\ndesign → build → verify — and is the judgment layer around it: what to\ndecide, what to ask, and what makes each artifact good. It deliberately\ndiverges from the prompt\'s literal call sequence where the tools require\nit (members added early, `skip_default_columns`, column types).\n\n**Covers:** brief intake, workspace-vs-board decision, creation calls and\nmembership, column design, definition authoring, seed notes/channels/\nresources, git repo binding, the starter backlog, verification, and\nre-init on an existing workspace.\n\n**Not for:** pipeline configuration, team/agent binding, or runner\nlaunch — bootstrap only leaves the board *shaped* so that handoff needs\nno migration; writing PLAN/RESEARCH grounding documents — bootstrap just\npoints the definition at where they (will) live; or day-to-day board\nusage after init — that is the MCP Coordination Rulebook (this skill\nobeys its write-and-verify discipline throughout).\n\n## 1. Brief intake — extract, then ask only the gaps\n\nFrom the brief/context, extract: **product goal**, **tech stack**,\n**team** (names, emails, roles), **repos** (URL, default branch),\n**explicit no-goes**, and the **workflow shape** — kanban-ish? a custom\nlifecycle (e.g. a governance flow)? plausibly runner-operated later?\nTimeline/milestones and key external contacts if present.\n\nAsk the user **only what the brief genuinely lacks and the platform\ncan\'t answer** — and write every answer into the definition (step 5),\nnever just into the conversation. Before asking, check whether an\nexisting workspace/board already records it (`get_project_context`,\n`get_definition`). **Never invent a missing project noun** (stack, repo,\nmembers) from the project type — if the brief and the platform don\'t\nanswer it, ask.\n\n## 2. Workspace vs board\n\n- **New workspace** per client/program; **new board** per project. A\n  workspace groups the boards, notes, channels, resources, and members\n  that share a context.\n- **One board per repo — permanent rule.** The runner\'s repo-wide gates\n  make sharing a repo across active boards a deadlock; adopt the rule\n  everywhere so no board ever needs untangling. Before binding a repo,\n  check it isn\'t bound to another active board (`list_git_repos` across\n  the workspace\'s boards).\n- Workspace slugs are globally unique; board slugs are unique per\n  workspace and must match `^[a-z0-9]+(-[a-z0-9]+)*$`. `create_board` is\n  idempotent **only when you pass an explicit `slug`** (on collision the\n  backend silently returns the existing board); a slugless re-run mints\n  a `-2` sibling instead. `create_workspace` derives its slug from the\n  name when omitted and reports the existing workspace on collision — it\n  never duplicates. Which is why step 9 (read before create) matters.\n\n## 3. Create + members\n\n1. `create_workspace` (name; slug auto-derives) or reuse the existing\n   one.\n2. `create_board` (name, description, and an explicit `slug` — the\n   idempotency key, section 2). **Pass `skip_default_columns=true`\n   whenever you design columns yourself** — otherwise the platform\n   auto-creates four default columns (To Do / In Progress / Blocked /\n   Done), already typed backlog/active/blocked/done, and they will sit\n   beside yours as duplicate scheduler-visible lanes. The tool also accepts\n   the definition fields inline (the section-5 table), so board +\n   definition can land in one call on a **first** init — but those\n   fields RE-PUT on every re-run, so keep them out of `create_board`\n   whenever the board might already exist (caution in section 9).\n3. `add_workspace_member` per person — it takes an email and\n   **auto-provisions the user if none exists**, so a typo silently\n   creates a phantom member: copy emails exactly from the brief. Roles:\n   `owner`, `admin`, `member` (default), `viewer`.\n4. Re-read (`get_workspace`, `get_board`) before moving on — 2xx ≠\n   persisted.\n\n## 4. Columns — match the workflow, decide runner-readiness now\n\nColumn design is one decision: **could this board EVER be\nrunner-operated?**\n\n- **Plausibly yes** (the brief mentions automation, agents, or the\n  project ships code): use the canonical typed set from day one —\n  `backlog`, `active`, `review`, `done` (optionally `blocked`,\n  human-managed), each created with its `column_type`. Types are the\n  runner scheduler\'s discovery key; retrofitting them later is a\n  migration. Valid `column_type` values are enforced server-side\n  (`backlog`/`active`/`review`/`done`/`blocked`, or omitted = untyped);\n  invalid values are rejected.\n- **Human-only** (docs, governance, design crit): shape the columns to\n  the team\'s real lifecycle — names are free, types optional. Untyped\n  columns are human zones: invisible to runner roles and excluded from\n  pickup-style searches; that\'s a feature for parking lots, a bug for\n  work the team expects agents to see. Decision rule: **type any column\n  whose cards future agent sessions should find via search; leave\n  untyped only true parking lots** (ideas, someday, blocked-on-client).\n\n`create_column` per column; `reorder_columns` takes the **complete**\nordered list of column ids. Verify the types landed by re-reading\n`get_board` — a column with a missing type is a silent dead zone later.\n\n## 5. The definition — the core deliverable\n\nThe definition is what makes every other workflow portable: agents read\nproject nouns from it at runtime instead of asking or assuming. A\nbootstrap that leaves it thin has failed regardless of how nice the\nboard looks. Write it with `update_definition` — `scope` plus a\ndedicated param per field below; `content` is only a passthrough for\nkeys not yet exposed as params. (On a first init the same fields can\nride inline on `create_board`; on any possible re-run prefer\n`update_definition` — section 9.) Field by field:\n\n| field | what belongs there | quality bar |\n|---|---|---|\n| `scope` | one paragraph: what this project is and how it\'s coordinated | a stranger knows what the board is for |\n| `objectives` | the brief\'s goals, each with a `priority` | measurable or at least checkable |\n| `exclusions` | the explicit no-goes | every "we will NOT…" from the brief lands here |\n| `tech_stack` | languages, frameworks, infra | what an agent needs to write idiomatic code |\n| `constraints` | conventions, process rules, hard limits | each one actionable ("PRs need review before done") |\n| `stakeholders` | people with `role`; link a `channel_id` for their contact entry | covers everyone named in the brief |\n| `decisions` | choices already made, each with `rationale` | future sessions stop re-litigating them |\n| `references` | repo paths, doc locations (the plan docs once they exist), external links | where the durable documents live |\n| `custom_fields` | board conventions: epic-label scheme, grouping concepts | how cards are organized on *this* board |\n| `coding_standards` | project-specific style notes (free text) | only what differs from house defaults |\n| `milestones` | dated phases if the brief has them | — |\n\nUpdates are shallow-merge with whole-list replacement per field — on a\nre-init, read first and send complete lists. Re-read after writing and\nconfirm the fields before reporting.\n\n## 6. Seed content\n\n- **One pinned orientation note** (`create_note`, `pinned=true`,\n  board-scoped): where things live, how the board flows, where decisions\n  go. Markdown content is accepted and normalized. Pin sparingly —\n  pinned notes are what every future session reads first.\n- **Channels** (a workspace contact registry, not a chat room): default\n  to **none**; create one per external contact point the brief actually\n  names (the client\'s email, the team\'s Slack), and link them from the\n  definition\'s `stakeholders`. Don\'t scaffold empty channels.\n- **Git repo binding** (`create_git_repo`: name, url, provider,\n  `default_branch`): bind the named repo, one board per repo (step 2).\n  Two non-obvious arguments: `require_branch_protection` **defaults to\n  true** (it arms branch protection on the default branch at first\n  runner clone) — pass `false` for human-owned/legacy repos, and the\n  runner standing rule keeps it `false` there too; `integration_branch`\n  is not settable at create — leave it (null ⇒ default branch) and set\n  it via `update_git_repo` only if the project uses a dedicated\n  integration branch, or leave that to the runner-preparation pass.\n- **Resources** for files the brief came with: `get_upload_url` →\n  upload → `create_resource` with the returned path.\n\n## 7. Starter backlog\n\nCreate the initial cards with `bulk_create_cards` (batched calls beat a\ncreate loop — burst mutations rate-limit; **max 50 cards per call, and\none invalid `column_id` rejects the whole batch**) into the\nbacklog/intake column:\n\n- Use the house card anatomy — Context / Goal / Scope / Acceptance\n  Criteria / Test-First DoD / Likely Files / Out of Scope / Depends On —\n  in each card description. For a human-only board the same structure\n  still reads well; trim what has no meaning there (e.g. Likely Files\n  for non-code work).\n- Set `card_type` (`task`/`issue`/`feature`/`bug`) and `priority`\n  (`none`/`low`/`medium`/`high`/`urgent`) on every card.\n- **Never pre-seed routing labels** — they are runner routing signals\n  owned by the pipeline\'s label flow, and pre-seeding strands cards.\n  Epic/milestone labels (`M1`, `epic-payments`) are organizational and\n  fine — record the scheme in the definition\'s `custom_fields`.\n- If order matters from day one: `bulk_set_card_dependencies`, then\n  `validate_board_dependencies` (cycles/orphans) before reporting.\n\n## 8. Verify, then report\n\nRe-read everything you created — `get_board` (columns + types + cards),\n`get_definition` (fields), the note and repo — and report **from the\nre-read state**, marking what was newly created vs already existed. A\nsummary table (entity → id → status) closes the init.\n\n## 9. Re-init: extend, don\'t duplicate\n\nWhen bootstrap is requested on a workspace that may already be\ninitialized, **read before creating**: `get_project_context` /\n`list_boards` first (the context call already returns existing notes and\nrepos; spot-check seed content with `list_notes` / `list_channels`). If\nthe project\'s board exists:\n\n- extend the definition by read-modify-write (complete lists, section 5),\n- add only missing members/columns/cards,\n- never create a sibling board, a second repo binding, or a duplicate\n  orientation note.\n\nBlind re-runs don\'t fail loudly: an explicit-slug `create_board`\nsilently hands you the existing board, and a slugless one silently mints\na `-2` sibling. Worse, definition fields passed to `create_board` are\nRE-PUT even on the collision path — every field you send overwrites that\nfield on the existing board\'s definition wholesale (fields you omit\nsurvive), silently rolling back anything recorded there since init. On\nre-init, never resend definition fields through `create_board`;\nread-modify-write via `update_definition` instead. Detecting "already\nexists" is your job, from reads, not from status codes.\n\n## 10. Handoffs\n\n- Project going agentic → the runner-preparation workflow (workspace\n  parity, team/agent binding, card-DAG authoring, launch). Bootstrap\'s\n  part of that contract: typed columns, one-board-per-repo, zero\n  pre-seeded routing labels.\n- The run needs grounding docs first → author the PLAN/RESEARCH\n  documents in the repo, and record their paths in the definition\'s\n  `references` once they exist.\n- Everything after bootstrap — claiming cards, notes, write discipline —\n  → the MCP Coordination Rulebook.\n',
        ),
    ),
)
